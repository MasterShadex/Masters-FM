==
== V14_S7_30_6_LOG.md  --  Stage 7.30.6 running log
== customize.html: TOOLTIP SYSTEM (info-icon hover/focus/tap help, research-first from legacy)
== Force-added past V*_LOG.md gitignore.
==

# STEP 0 -- checkpoint + research-first (legacy help extraction + tooltip set lock)

## S0.0 Pre-conditions

| Check | Expected | Actual |
|---|---|---|
| HEAD | 7.30.5 closure `515300d` + operator toggle-removal `7358577` | HEAD = 7358577 (baseline incl. toggle removal) |
| `src/customize.html` SHA256 | post-toggle-removal | `072D9F7CF08898DF...` |
| `src/customize_legacy.html` | LEGACY_SHA `7E98377D...` | MATCH (read-only source of help text) |
| `src/overlay.html` | `9A7CC817...` | MATCH |
| Protected (tray.ps1 / tray_native.cs / launcher.cs / server.js) | unchanged | all 4 MATCH |
| version.json | 14.0.0 | 14.0.0 (toggle-removal cold rebuild bvhlwoah2 in flight; restore if it stamps msi_sha256) |
| Strikes | 0 / 3 fresh | YES |

NOTE: brief pre-condition said HEAD=515300d, but the operator's direct toggle-removal
request landed first (commit 7358577 on top of 515300d). 7.30.6 baselines on 7358577.

## S0.1 Disk backup
`_BACKUPS_2026-05-29_22-06_S7_30_6_PRE/`: `customize_pre.html` + `head_sha.txt` (7358577).

## S0.2 Protected files SHA256 baseline
tray.ps1 `19011F0B...` / tray_native.cs `6B9804A1...` / launcher.cs `291ED4C9...` /
server.js `C15ED931...` -- all MATCH 7.30.5 baseline (unchanged).

## S0.3 RESEARCH -- legacy help mechanism + extraction

**Mechanism:** legacy `customize_legacy.html` stores per-control help as POSITIONAL
`<p class="control-help">...</p>` paragraphs placed AFTER each control's row (51 total).
Concise, operator-friendly, always-visible. v2 has NO help/description field yet
(grep count 0) -> Stage 7.30.6 adds an additive `help` field + moves the text into
hover tooltips (operator "less text" goal).

**Coverage:** 51 legacy control-help paragraphs extracted. Mapped legacy id -> condensed
tooltip below. Self-explanatory ones (help merely restates the label) are marked SKIP
per operator decision #2; jargon / non-obvious / dependency / example help is WARRANTED.

## S0.4 LOCK -- tooltip set (WARRANTED, ~40 of 120)

Legacy IDs (v2 SETTINGS_CONFIG IDs match the legacy c-* IDs; STEP 1 verifies each
exists before adding `help`). Condensed tooltip text (operator refines at gate per
decision #3):

MASTERS
- c-master-overall-size -- "Scales the whole card. 100% is standard; per-element sizes stay relative."
- c-master-text-size    -- "Scales all text at once -- the title stays larger than the artist."
- c-master-glow         -- "Master switch for every glow. Off = flat look; per-element glow settings are kept."
- c-master-animations   -- "Master switch for every animation. Clock time always stays smooth."

LOOK / CARD
- c-opacity      -- "How see-through the whole overlay is. 100 = fully solid."
- c-card-angle   -- "Direction of the color blend across the card."
- c-card-blur    -- "Only works if Card transparency is below 100."
- c-card-opacity -- "How see-through the card is. Lower lets Blur-behind-card show through."
- c-card-radius  -- "How rounded the card corners are. 0 = sharp."

EFFECTS (glow sizes + border)
- c-np-glow-size       -- "Soft halo around the Now Playing label. Set to 0 to hide."
- c-platform-glow-size -- "Soft halo around the platform badge. Set to 0 to hide."
- c-title-glow-size    -- "Soft halo around the track title. Set to 0 to hide."
- c-artist-glow-size   -- "Soft halo around the artist name. Set to 0 to hide."
- c-ts-glow-size       -- "Soft halo around the time text. Set to 0 to hide."
- c-border-w           -- "How thick the spinning border ring is. 0 hides it."

TEXT (track + artist)
- c-np-text         -- "The label text. Common: Now Playing, On Air, or anything you like."
- c-title-weight    -- "Heavier = thicker letters."
- c-title-mspd      -- "Marquee speed: pixels/sec when the title is too long to fit."
- c-title-mpause    -- "Pause at each end of the marquee scroll."
- c-title-spacing   -- "Extra space between each letter."
- c-artist-weight   -- "Heavier = thicker letters."
- c-artist-mspd     -- "Marquee speed: pixels/sec when the name is too long to fit."
- c-artist-mpause   -- "Pause at each end of the marquee scroll."
- c-artist-spacing  -- "Extra space between each letter."
- c-platform-sc-label -- "Override the badge text when SoundCloud is the source."

AUDIO (spectrum -- the jargon cluster)
- c-spec-autogain -- "Normalizes loud and quiet music to similar bar heights."
- c-spec-gain     -- "Boosts quiet music. 1.0x = no boost."
- c-spec-mode     -- "Rainbow cycles hues; Solid uses one color; Gradient blends three."
- c-spec-color    -- "Used by the Solid and Gradient color modes."
- c-spec-mirror   -- "Makes the bar pattern symmetric around the center."
- c-spec-opacity  -- "How see-through the bars are."
- c-spec-bars     -- "More bars = finer frequency detail."
- c-spec-gap      -- "Gap between bars. 0 for a solid block."
- c-spec-radius   -- "Rounded vs. flat bar tops."
- c-spec-min      -- "Shortest bar height even when quiet. 0 for full reactivity."
- c-spec-height   -- "How tall bars can grow on loud peaks."
- c-spec-response -- "Lower = snappier, closer to real-time. Higher = lazier, less CPU."
- c-spec-smooth   -- "Lower = punchier transients. Higher = silkier flow."
- c-spec-fps      -- "How often bars redraw per second. Match your monitor (60 / 144 / 240)."

LAYOUT / ANIM
- c-anim-dur   -- "How long the slide-in animation lasts."
- c-anim-slide -- "How far the card travels before landing."
- c-anim-ease  -- "Animation curve. Spring overshoots and settles; Ease glides; Linear is constant."

**SKIP (self-explanatory -- legacy help merely restated the label):**
c-master-accent (operator's explicit example), c-card-top, c-card-bot, c-title-color,
c-artist-color, c-title-size, c-artist-size, c-spec-on, c-prog-on, c-art-position,
c-anim-dir (dropdown options already say which side). All other ~80 entries had NO
legacy help -> no tooltip.

LOCKED SET: ~40 tooltips (exact IDs above), pending per-ID existence check in v2
SETTINGS_CONFIG at STEP 1. Counts to stay unchanged: 120 entries / FLAT_TO_NESTED_MAP
118 / THEMES 22 (help is an additive field only).

## S0.6 Commit
STEP 0 deliverable: this log. No source touched.
Commit: `Stage 7.30.6: STEP 0 -- research-first help text extraction + tooltip set locked`

== END OF STEP 0 ==

# STEP 1 -- Item 2: additive help field on warranted entries

Added `help: '...'` to 41 existing SETTINGS_CONFIG entries (the S0.4 locked set),
inserted between the `id` and `type` keys. IDs verified against v2 inventory; ID
corrections vs S0.4 draft: c-opacity -> c-overlay-opacity, c-master-anim ->
c-master-animations, c-spec-gain -> c-spec-sensitivity, c-anim-ease ->
c-anim-easing; dropped c-border-w (no such v2 entry).

SE1 structural check:
- `help: '` count = 41 (exactly the warranted set; was 0 before).
- `{ id: 'c-` count = 120 (entry count UNCHANGED -- additive only, no new entries,
  no config-key changes).
- FLAT_TO_NESTED_MAP + THEMES untouched (runtime self-test at STEP 3 confirms).

Set breakdown: 4 masters / 5 look-card / 5 glow sizes / 9 text+marquee /
1 platform label / 14 spectrum / 3 anim = 41. Text condensed from legacy
control-help (S0.3); operator refines wording at gate.

Commit: `Stage 7.30.6: STEP 1 -- additive help field on 41 warranted SETTINGS_CONFIG entries`

== END OF STEP 1 ==

# STEP 2 -- Item 3: tooltip UI (accessible info icon + positioned tooltip)

Edits to src/customize.html:
- renderControl: `if (config.help) label.appendChild(createHelpIcon(config))`.
- `createHelpIcon(config)`: real `<button class="help-icon">` (keyboard-focusable,
  aria-label "Help: <label>") with an inline SVG info glyph + a child
  `<span class="help-tip" role="tooltip">` carrying the help text.
- `initHelpTooltips()`: delegated click -> tap-toggle `.active` (one-at-a-time);
  click-away closes; Esc closes + blurs focused icon. Hover/focus handled by CSS.
  Wired in init() after initSupercatCollapse().
- CSS: `.control-row { position: relative }` (anchor); `.help-icon` (muted, ->
  accent on hover/focus); `.help-tip` opens BELOW the row, `left:0`,
  `max-width: calc(100% - 8px)` so it CANNOT clip the sidebar right edge; dark
  surface (--c-surface-elevated) + border + shadow; shown on hover / focus / .active.

SE1 (preview MCP @ 127.0.0.1:8799, all supercats expanded):
- SETTINGS_CONFIG.length = 120 (parse OK); help fields = 41; .help-icon = 41;
  .help-tip = 41 -- icons render ONLY where help present.
- Self-explanatory c-spec-on has NO icon (correct).
- Binding intact: c-spec-response slider type=range; its tip = "Lower = snappier...".
- NO-CLIP: activated tip right edge 291 <= row right 299; on-screen (viewport 1280).
- Tap: tap A opens A; tap B closes A + opens B (one-at-a-time); Esc closes all;
  click-away closes. Console 0 warn / 0 error. round-trip self-test ok.

Commit: `Stage 7.30.6: STEP 2 -- tooltip UI (accessible info icons + no-clip positioned CSS tooltip + tap/esc JS)`

== END OF STEP 2 ==
