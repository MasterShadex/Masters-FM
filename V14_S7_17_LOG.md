# Stage 7.17 -- local v14.0.0 cut (running log)

**Brief:** `CLAUDE_CODE_INSTRUCTIONS.md` (Stage 7.17, ~18 KB / 463 lines)
**Pattern:** Process-only brief. No code changes. Only `version.json` edits.
**Baseline HEAD:** `e01603e` (Stage 7.16 STEP 9 closure)
**Author:** Ruflo (Claude) -- autonomous brief execution
**Started:** 2026-05-20

---

## STEP 0 -- Checkpoint + state inventory

### S0.0 Pre-conditions (all PASS)

| Check | Expected | Observed |
|---|---|---|
| HEAD | Stage 7.16 closure | `e01603e49a544a785095b81913f057f1a833512f` |
| `version.json` version field | `14.0.0-rc.3` | `14.0.0-rc.3` |
| `git status` source code | clean | clean (only pre-existing carry-over noise: `version.json` `msi_sha256` rewrite + `.claude/scheduled_tasks.lock`) |
| Protected files SHA256 | match baseline | all 4 MATCH (see S0.2) |
| memory.md has Stage 7.16 entry | yes | yes (closure entry from `e01603e`) |
| In-flight orphan work | none | none -- all 123 commits ahead of origin belong to v14 stages 7.7B → 7.16 |
| Deferred items still deferred | yes | server log rotation NOT done, Fix C NOT done, preview tick NOT done, customize HISTORICAL_NOTES extraction NOT done |

### S0.1 Disk backup

```
G:\Project Folder\Master FM\_BACKUPS_2026-05-20_22-55_S7_17_V14_CUT_PRE\
  src_snapshot\              <-- full src/ tree (PowerShell Copy-Item -Recurse, 29 top-level items)
  version_pre_cut.json       <-- byte-identical pre-edit copy of version.json (rc.3 state)
  head_sha.txt               <-- e01603e49a544a785095b81913f057f1a833512f
```

### S0.2 State inventory (the "before" snapshot)

#### Current HEAD

`e01603e49a544a785095b81913f057f1a833512f` -- Stage 7.16 STEP 9 -- *memory.md APPEND + overlay.html v14 rebuild complete*

#### Current `version.json` content

```json
{"msi_sha256":"fe5363438ab012f0e1ec1a5bf6d936c8dd03c30588d6ec43ca6da26b3f5876b4","autoInstall":false,"version":"14.0.0-rc.3","msi_url":"https://github.com/MasterShadex/Masters-FM/releases/download/v14.0.0-rc.3/Masters-FM-V14.0.0-rc.3.msi"}
```

Fields:
- `msi_sha256`: `fe5363438ab012f0e1ec1a5bf6d936c8dd03c30588d6ec43ca6da26b3f5876b4` (will be overwritten by rebuild script in STEP 2)
- `autoInstall`: `false` (preserve)
- `version`: `14.0.0-rc.3` -- **target of STEP 1 edit**
- `msi_url`: GitHub Releases URL pointing at the rc.3 draft asset (preserve -- references the historical artifact; updating it would imply we're pushing somewhere, which rule 2 forbids)

No `channel` / `track` field is present. No edit needed there.

#### Commits ahead of origin/main

**123** -- never pushed across the entire v14 cycle per operator standing rule.

#### Total commits in repo

**339**

#### Latest Stage 7.16 commit

`e01603e` (= current HEAD) -- the Stage 7.16 STEP 9 closure.

#### Protected files SHA256 (matching `V14_S7_REPLAN_PROTECTED_BASELINE.md`)

| File | Expected (baseline) | Observed |
|---|---|---|
| `src/tray.ps1` | `19011F0B...09533F` | `19011f0bd093cea51cb34d053209f33fb3a37de673777bab34b5f8f26609533f` MATCH |
| `src/tray_native/tray_native.cs` | `6B9804A1...FA9148` | `6b9804a1ab70000652a2754e886be3f05167f40ec136eb2cc6cdd62d8efa9148` MATCH |
| `src/launcher.cs` | `291ED4C9...E0BD9D` | `291ed4c92b9bea391ba9204323ea41ba60ad7903af6e6d7ba9404e1056e0bd9d` MATCH |
| `src/server.js` | `C15ED931...02A16AF` | `c15ed9310cb33044a090878918dc2b89b3fb843901ba0f199d3092ef502a16af` MATCH |

All 4 UNCHANGED across the ENTIRE v14 cycle (Stage 7.7B through 7.16). Stage 7.17 will not touch any source file; STEP 4 re-checks.

#### Currently-installed DLL ProductVersion

`14.0.0-rc.3+9ced67467bf601f3bdbf3a2b6dd1837bf4bd4235`

This is from the Stage 7.16 STEP 9 dual-build install (commit `9ced674` is the last source-affecting commit; `e01603e` is the docs-only STEP 9 commit). The `+9ced67467b...` suffix is git-SHA metadata; the version portion `14.0.0-rc.3` is what STEP 2's rebuild needs to flip to `14.0.0`.

### S0.3 No in-flight orphan work

`git log origin/main..HEAD --oneline` (123 commits) inspected. Every commit is part of a documented v14 stage (7.7B / 7.8B-D / rc.x ship-prep / 7.11 / 7.12 A-C / 7.13 / 7.14 / 7.15 / 7.16). No orphan experiments, no half-done features, no test commits. Safe to roll into the v14.0.0 cut.

`git status` source-tree changes: none. Only the pre-existing carry-over (`version.json` `msi_sha256` rewrite by Stage 7.16 rebuilds + `.claude/scheduled_tasks.lock` harness file). These are NOT source code drift.

### S0.4 Commit

Files staged: `V14_S7_17_LOG.md` only (no source edits in STEP 0).

Commit message: `Stage 7.17: STEP 0 -- checkpoint + state inventory pre-v14.0.0 cut`

**Committed:** `bfec846` -- *Stage 7.17: STEP 0 -- checkpoint + state inventory pre-v14.0.0 cut*

---

## STEP 1 -- version.json bump

Edited `version.json`:

```
-{"msi_sha256":"...","autoInstall":false,"version":"14.0.0-rc.3","msi_url":"https://github.com/MasterShadex/Masters-FM/releases/download/v14.0.0-rc.3/Masters-FM-V14.0.0-rc.3.msi"}
+{"msi_sha256":"...","autoInstall":false,"version":"14.0.0","msi_url":"https://github.com/MasterShadex/Masters-FM/releases/download/v14.0.0-rc.3/Masters-FM-V14.0.0-rc.3.msi"}
```

ONE intentional change (version field). The `msi_sha256` difference in git diff was pre-existing Stage 7.16 carry-over (recorded in S0 inventory) which the STEP 2 rebuild overwrote anyway. The `msi_url` was preserved (still pointing at rc.3 release URL -- updated automatically by the rebuild script during STEP 2; see below).

UTF-8 BOM preserved (existing file format).

**NOT committed.** Per brief S1.3, the v14.0.0 commit waits for STEP 4 after the rebuild produces matching MSI sha.

---

## STEP 2 -- Fresh full rebuild (first pass)

### S2.1 Run

`_full_rebuild.ps1` ran as background task `bl62gc551` (22:58:04 -> 23:08:40 local, ~10:36 cold). Exit 0. `REBUILD DONE OK` confirmed. MSI signed `CN=MasterShadex`. Installed OK. Tray + server + audio_spectrum relaunched.

### S2.3 Critical check -- FAILED

```
Installed ProductVersion: 14.0.0-rc.3+bfec8469fcdc4150ed31efdf7519f560392e34e1
```

The `rc.3` is STILL present despite `version.json` correctly reading `14.0.0`. **HALTED before printing the gate** per S3.3 FAIL handling. Investigated per ROLLBACK section.

### Diagnosis

`grep -nE "14\.0\.0-rc\.3|14\.0\.0-RC\.3" ...` found 6 hardcoded `14.0.0-rc.3` strings across 4 source files:

| File | Line | Current | Effect |
|---|---:|---|---|
| `_full_rebuild.ps1` | 287 | `$appVer = '14.0.0-rc.3'` | Build script fallback when version.json read fails (never hit when version.json is readable) |
| `MastersFM_Tray_v14.csproj` | 22 | `<Version>14.0.0-rc.3</Version>` | Drives `AssemblyVersion` -- doesn't directly affect ProductVersion |
| `MastersFM_Tray_v14.csproj` | 25 | `<InformationalVersion>14.0.0-rc.3</InformationalVersion>` | Drives `AssemblyInformationalVersion` -> `ProductVersion` -- **the actual culprit** |
| `TrayMenuViewModel.cs` | 46 | `_nowPlayingHeaderText = "v14.0.0-rc.3 - ready"` | Tray menu idle text |
| `TrayMenuViewModel.cs` | 153 | `? "v14.0.0-rc.3 - ready"` (ternary) | Tray menu idle text (fallback when artist empty) |
| `App.xaml.cs` | 161 | `UserAgent.ParseAdd("MastersFM/14.0.0-rc.3 (UpdateCheck)")` | Update-check HTTP User-Agent |

Plus ~30 documentation references across 10 `.md` files (all historical context: release notes, audit reports, prior stage closure reports, memory.md APPEND entries) -- NOT to be edited per "describe what was at the time" rule.

---

## STEP 2.5 -- Brief correction (operator-authorized version-string edits)

Operator confirmed: "ABSOLUTE RULE 1 was too strict; version-string edits in `_full_rebuild.ps1` and `MastersFM_Tray_v14.csproj` are now explicitly authorized... Constraint stays: ONLY the version string changes -- no other logic edits to those files." Then explicitly authorized the 3 additional runtime-impacting refs after I surfaced them via AskUserQuestion.

**6 version-string edits / 4 files, 6 insertions + 6 deletions:**

```diff
-_full_rebuild.ps1:287    $appVer = '14.0.0-rc.3'
+_full_rebuild.ps1:287    $appVer = '14.0.0'

-MastersFM_Tray_v14.csproj:22    <Version>14.0.0-rc.3</Version>
+MastersFM_Tray_v14.csproj:22    <Version>14.0.0</Version>

-MastersFM_Tray_v14.csproj:25    <InformationalVersion>14.0.0-rc.3</InformationalVersion>
+MastersFM_Tray_v14.csproj:25    <InformationalVersion>14.0.0</InformationalVersion>

-TrayMenuViewModel.cs:46    [ObservableProperty] private string _nowPlayingHeaderText = "v14.0.0-rc.3 - ready";
+TrayMenuViewModel.cs:46    [ObservableProperty] private string _nowPlayingHeaderText = "v14.0.0 - ready";

-TrayMenuViewModel.cs:153            ? "v14.0.0-rc.3 - ready"
+TrayMenuViewModel.cs:153            ? "v14.0.0 - ready"

-App.xaml.cs:161            hc.DefaultRequestHeaders.UserAgent.ParseAdd("MastersFM/14.0.0-rc.3 (UpdateCheck)");
+App.xaml.cs:161            hc.DefaultRequestHeaders.UserAgent.ParseAdd("MastersFM/14.0.0 (UpdateCheck)");
```

**STEP 2.5 rebuild** (`bq5eperco`, ~38 min wall-clock due to mid-rebuild idle, completed 00:34:25 next day local). Exit 0. `REBUILD DONE OK`.

Verification:

| Check | Pre-S2.5 | Post-S2.5 |
|---|---|---|
| Installed `ProductVersion` | `14.0.0-rc.3+bfec846...` | `14.0.0+bfec8469fcdc4150ed31efdf7519f560392e34e1` ✓ |
| `version.json` `version` | `14.0.0` | `14.0.0` (unchanged) |
| `version.json` `msi_sha256` | (carry-over) | `e7744565f51b685da542c9192be9455bf9912cc2974d98d806c88fbed39d934b` (fresh, from this rebuild) |
| `version.json` `msi_url` | `.../v14.0.0-rc.3/...` | `.../v14.0.0/Masters-FM-V14.0.0.msi` (auto-updated by rebuild script based on new version) |

**Strikes:** 0 (operator-classified as brief correction, not Ruflo strike).

---

## STEP 3 -- Internal sanity check + verification gate (redux)

### S3.1 Sanity check

| Process | Status |
|---|---|
| `MastersFM_Tray` (PID 25044) | running |
| `server` (PID 4216) | running |
| `audio_spectrum` (PID 37700) | running |
| HTTP `GET /current` | 200 OK (43411 bytes) |

### S3.2 Gate -- PASS at first attempt

Operator replied **PASS** to the redux gate. About dialog confirmed reads `14.0.0`, no `rc.3` visible.

---

## STEP 4 -- Post-gate + v14.0.0 commit

### S4.1 Protected files SHA256 recheck

All 4 protected files SHA256 MATCH `V14_S7_REPLAN_PROTECTED_BASELINE.md` -- unchanged across the entire v14 cycle (Stage 7.7B -> 7.17).

### S4.2 v14.0.0 commit

Staged: `version.json`, `_full_rebuild.ps1`, `src/tray_csharp/App.xaml.cs`, `src/tray_csharp/MastersFM_Tray_v14.csproj`, `src/tray_csharp/ViewModels/TrayMenuViewModel.cs`.

```
[main 718e3e1] v14.0.0 -- local cut
 5 files changed, 7 insertions(+), 7 deletions(-)
```

Exact commit message per brief: `v14.0.0 -- local cut`.

### S4.3 Verify

`git show HEAD --stat`: 5 files changed, 7/7 insertions/deletions. Only the 5 expected files touched. Commit message matches brief.

### S4.4 NO PUSH verification

| Check | Result |
|---|---|
| `git status --branch` | `main...origin/main [ahead 125]` |
| `git rev-list --count origin/main..HEAD` | 125 (was 123 pre-Stage 7.17; +1 STEP 0 + +1 v14.0.0 cut = 125) |
| Push command run | NEVER |
| Tag command run | NEVER |
| GitHub interaction | NONE |

### S4.5 Final dual-build verification

`_full_rebuild.ps1` ran as background task `bvu1ufofu` (~30-40 s warm-cache). Exit 0. Installed `ProductVersion`: `14.0.0+718e3e17bbf180834ed46fcaa2833c1e719715e9` -- now embeds the v14.0.0 commit SHA. MSI sha256: `34dbf7b2122174dca74896957b105bde9286f4443fe21b3b1ea4717293803d5b`. This is the canonical v14.0.0 MSI.

### S4.6 V14_S7_17_REPORT.md

Written to `G:\Project Folder\Master FM\V14_S7_17_REPORT.md` (gitignored deliverable; not staged for commit). 14 sections.

### S4.7 md/memory.md APPEND

Appended Stage 7.17 closure entry to `md/memory.md`. Closes the v14 work cycle.

### S4.8 Commit

Files staged: `md/memory.md` + `V14_S7_17_LOG.md`. `V14_S7_17_REPORT.md` intentionally NOT staged (gitignored deliverable). Working-tree `msi_sha256` carry-over from the dual-build NOT staged (per rule pattern).

**Committed:** *<pending>* -- *Stage 7.17: STEP 4 -- memory.md APPEND + v14.0.0 cycle closure*

---

## Definition of Done -- final tally

| # | Check | Result |
|---:|---|---|
| 1 | Backup checkpoint created | YES (`_BACKUPS_2026-05-20_22-55_S7_17_V14_CUT_PRE/`) |
| 2 | Pre-cut state inventory recorded | YES (S0.2 + log §S0.2) |
| 3 | No in-flight orphan work (git log clean) | YES (S0.3) |
| 4 | version.json bumped 14.0.0-rc.3 -> 14.0.0 | YES (S1.1) |
| 5 | Fresh _full_rebuild.ps1 clean | YES (S2.1 first pass + S2.5 redux + S4.5 dual-build, all exit 0) |
| 6 | Installed DLL ProductVersion shows 14.0.0 (no rc.3) | YES (S2.3 redux -- `14.0.0+718e3e17bb...` after dual-build) |
| 7 | Internal sanity check: tray + server + spectrum all running | YES (S3.1) |
| 8 | Internal sanity check: HTTP server responding | YES (HTTP 200) |
| 9 | Operator verification gate PASSed | YES (first PASS at redux gate) |
| 10 | Protected files SHA256 still UNCHANGED | YES (S4.1) |
| 11 | v14.0.0 commit landed locally | YES (`718e3e1`) |
| 12 | NO git tag created | YES |
| 13 | NO git push executed | YES (125 commits ahead, unchanged) |
| 14 | NO GitHub interaction | YES |
| 15 | rc.3 draft on GitHub untouched | YES (no API call made) |
| 16 | Final dual-build clean (optional but recommended) | YES (S4.5 -- canonical v14.0.0 MSI produced) |
| 17 | V14_S7_17_REPORT.md written | YES (gitignored) |
| 18 | md/memory.md APPEND committed | YES (this STEP 4 commit) |
| 19 | No code files edited (only version.json) | **CORRECTED via STEP 2.5 brief correction**: 5 source files total touched (version.json + 4 source files for version-string-only edits). Operator-classified as brief correction, not Ruflo strike. |
| 20 | No deferred items accidentally bundled in | YES (only version-string edits authorized in STEP 2.5; all v14.1.0 candidates remain deferred) |

**19/20 strict PASS, 20/20 with STEP 2.5 brief correction note.** Strikes consumed: 0 / 24.

*Stage 7.17 closure. v14 work cycle CLOSED. v14.0.0 is local truth.*

