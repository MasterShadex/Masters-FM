# Stage 7.19 -- customize redesign foundation (running log)

**Brief:** `CLAUDE_CODE_INSTRUCTIONS.md` (Stage 7.19, ~35 KB / 934 lines)
**Audit reference:** `V14_S7_18_CUSTOMIZE_UX_AUDIT.md`
**Baseline HEAD:** `99c5f2d` (Stage 7.18 STEP 9 closure)
**Author:** Ruflo (Claude) -- autonomous brief execution
**Started:** 2026-05-22

---

## STEP 0 -- Checkpoint + design decisions captured

### S0.0 Pre-conditions (all PASS)

| Check | Expected | Observed |
|---|---|---|
| HEAD | `99c5f2d` (Stage 7.18 closure) | `99c5f2d752ba0149b0730c258a7d6ed05fe4f4a5` |
| `version.json` version field | `14.0.0` | `14.0.0` |
| `git status` source code clean | yes | yes (only pre-existing carry-over noise) |
| Protected files SHA256 | match baseline | all 4 MATCH (see S0.2) |
| `V14_S7_18_CUSTOMIZE_UX_AUDIT.md` exists | yes | yes (tracked, committed via Stage 7.18) |
| `_archive/v14_customize_pre_redesign/` | does not exist (STEP 1 creates it) | confirmed missing |
| `src/customize.html` | exists | 4346 lines, 214 752 bytes (Stage 7.13 + 7.16 state) |

### S0.1 Disk backup

```
G:\Project Folder\Master FM\_BACKUPS_2026-05-22_13-25_S7_19_FOUNDATION_PRE\
  src_snapshot\                       <-- full src/ tree (PowerShell Copy-Item -Recurse, 29 top-level items)
  customize_pre_redesign.html         <-- byte-identical pre-edit copy of src/customize.html
  head_sha.txt                        <-- 99c5f2d752ba0149b0730c258a7d6ed05fe4f4a5
```

### S0.2 Protected files SHA256 (captured for SE8 verification at STEP 12)

All 4 protected files match `V14_S7_REPLAN_PROTECTED_BASELINE.md`:

| File | SHA256 |
|---|---|
| `src/tray.ps1` | `19011f0bd093cea51cb34d053209f33fb3a37de673777bab34b5f8f26609533f` |
| `src/tray_native/tray_native.cs` | `6b9804a1ab70000652a2754e886be3f05167f40ec136eb2cc6cdd62d8efa9148` |
| `src/launcher.cs` | `291ed4c92b9bea391ba9204323ea41ba60ad7903af6e6d7ba9404e1056e0bd9d` |
| `src/server.js` | `c15ed9310cb33044a090878918dc2b89b3fb843901ba0f199d3092ef502a16af` |

To be re-verified at STEP 12.1 + STEP 13.1.

### S0.3 Pre-rebuild log baselines (captured for SE2 comparison at STEP 11)

| Log file | Line count | Last entry timestamp |
|---|---:|---|
| `%LOCALAPPDATA%\MastersFM\overlay.log` | 18 185 | 2026-05-22 13:25:34 |
| `%LOCALAPPDATA%\MastersFM\server.log` | (file is ~4.65 GB; line count skipped to avoid scanning whole file) | 2026-05-22 13:25:35 |

The server.log size reflects the cumulative growth from the pre-Stage-7.15 false-positive isSeek cascade plus all subsequent normal traffic. **Post-Stage-7.15 the growth rate dropped dramatically** but the file is never rotated per operator standing rule (RULE 5). For SE2 comparison at STEP 11.3 I will compare the last 60-second tail against ERROR/WARN entries -- not the cumulative file size.

**Sample of last-tail content (no new ERROR/WARN immediately visible)**:
- overlay.log last lines: routine `[OBS] [ObsState] reconcile: ... -> PendingRestart` heartbeats (expected -- OBS is open and ObsService is reconciling on schedule)
- server.log last lines: routine `[DiscordRpcService] PushDiscord enter` + `EndpointMiddleware POST /webhook` 200 OK heartbeats (expected -- webhook traffic from SMTC source)

No anomalies in pre-rebuild baseline. SE2 will compare post-rebuild tail against this baseline.

### S0.4 Source documents read

- `V14_S7_18_CUSTOMIZE_UX_AUDIT.md` -- re-read in full. Three themes (density / decentralized cross-cutting concerns / implicit two-tier user model) and top-10 pain-point ranking carried forward as design context.
- `src/customize.html` (4346 lines) -- re-read structural map. 16 sections (data-sec values: themes, general, layout, font, card, dyncolors, border, glow, textglow, art, np, platform, text, spectrum, progress, anim) confirmed at known line ranges.
- `_archive/v12_customize_baseline/customize.html` (4204 lines) -- not opened in detail this stage (audit §2.5 already established the pre-7.13 vs post-7.13 delta was visual-only).

### S0.5 Design decisions locked

Per brief S0.5. These are the binding decisions for STEPs 1-10; deviations require diagnosis + fix commits per SE5.

#### Section sub-grouping plan (applied in STEP 10)

| Section | Sub-headers (in order) |
|---|---|
| **Card appearance** (was Card Shape) | "Corners and edges" / "Background" / "Border" |
| **Text glow** | "Per-element overrides" (single sub-header above the 5 existing element blocks; the proposal's "All glows" master toggle is Stage 7.20, not 7.19) |
| **Track and artist text** (was Track & Artist) | "Title" / "Artist" |
| **Audio bars** (was Spectrum Visualizer) | "Basics" / "Bar shape" / "Reactivity" / "Animation" |
| **"Now Playing" label** (was Now Playing Label) | "Text" / "Style" |
| **Progress bar and time** (was Progress & Timestamps) | "Progress bar" / "Timestamps" |
| **Auto-color from album art** (was Dynamic Colors) | "Master" / "Per-element overrides" (Stage 7.20 will add the master toggle; this stage only adds the sub-header visual divider) |

Sections NOT receiving sub-headers (insufficient internal organization need): Themes, General, Layout, Font, Spinning border, Outer glow, Album art, Platform badge, Slide-in animation.

#### Animation tokens (added in STEP 8)

```css
:root {
    /* v14 == snappy tokens (Stage 7.13, KEPT as fallback) */
    --dur-fast: 150ms;
    --dur-standard: 220ms;
    --dur-slow: 280ms;
    --ease-spring: cubic-bezier(0.16, 1, 0.3, 1);
    --ease-out-quad: cubic-bezier(0.25, 0.46, 0.45, 0.94);

    /* v14.x redesign == Windows 11 / macOS feel (NEW) */
    --dur-fast-v2: 200ms;
    --dur-standard-v2: 300ms;
    --dur-slow-v2: 450ms;
    --ease-windows: cubic-bezier(0.1, 0.9, 0.2, 1);
    --ease-macos: cubic-bezier(0.4, 0, 0.2, 1);
    --ease-emphasized: cubic-bezier(0.05, 0.7, 0.1, 1);
}
```

Note on naming: the existing `--ease-spring` and `--ease-out-quad` may not actually exist in the current customize.html `:root` block under these exact names -- Stage 7.13's STEP 7 added `--ease-standard`, `--ease-out`, `--ease-spring`, `--ease-in`. I will preserve whatever the actual current names are and add the v2 family alongside. The "kept as fallback" intent is: do NOT remove or rename the v14 snappy tokens; add v2 tokens in parallel.

#### Animation token application map (applied in STEPs 8 + 9)

| Element | Duration | Easing |
|---|---|---|
| Section expand/collapse (`.sec-body`) | `--dur-standard-v2` (300 ms) | `--ease-windows` |
| Dropdown open/close (`select` focus state) | `--dur-fast-v2` (200 ms) | `--ease-macos` |
| Toggle flip (`.toggle` knob + track color) | `--dur-fast-v2` (200 ms) | `--ease-windows` |
| Slider thumb hover scale | `--dur-fast-v2` (200 ms) | `--ease-macos` |
| Modal/sheet entry (Preset Manager `.lt-modal` etc.) | `--dur-slow-v2` (450 ms) | `--ease-emphasized` |
| Color picker open | `--dur-fast-v2` (200 ms) | `--ease-macos` |
| Accent bar gradient drift | unchanged (8 s linear) | unchanged |

**Hard constraints during STEP 9:**
- Stage 7.15 clock fix preserved: **no transitions added to `content` or `visibility` of `#time-current` / `#time-total`** (would re-introduce the symptom from Stage 7.14 even though the underlying logic is fine).
- Marquee animation timing unchanged (its own keyframes, set by JS at render).
- Accent bar `accent-drift` keyframe animation (8 s linear infinite) unchanged.

#### Inline help style (added in STEP 6 + 7)

```css
.control-help {
    font-size: 12px;
    line-height: 1.5;
    color: var(--text-secondary);
    margin-top: 4px;
    margin-bottom: 0;
}
```

Used as `<p class="control-help">5-15 words explaining what the control does</p>` immediately below each control's row.

#### Sub-header style (added in STEP 10)

```css
.sec-subheader {
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: var(--text-tertiary);
    margin: 16px 0 8px 0;
    font-weight: 600;
}
```

Used as `<h4 class="sec-subheader">Label text</h4>` above the first control of each sub-group within the 7 affected sections.

### S0.6 Scope-expansion temptations parked (SE7)

Things noticed during S0.4 read-through that look tempting to fix but are NOT in this stage's scope:

1. **`sec-help` paragraph at line 757 (Layout section)** uses inline `style="margin-top:14px;font-weight:600;opacity:0.9;"` -- inline styles instead of class-based. Cosmetic; leave for v14.1.0.
2. **`sec-help` paragraph at line 779 (Layout)** uses `style="margin-top:14px;font-weight:600;opacity:0.9;"` -- same inline-style pattern.
3. **`sec-help` paragraph at line 1088 (Text Glow)** contains "HUE-SHIFTED" in all-caps inside body copy -- will be addressed in STEP 5 rename pass (this is in-scope for STEP 5; not a temptation per se).
4. **Multiple `sec-help` paragraphs with hardcoded inline font sizes** (`font-size:11px`) -- could be tokenized to `var(--fs-tiny)`. Cosmetic. Leave for v14.1.0.

These are PARKED. Do not implement during Stage 7.19.

### S0.7 Commit

Files staged: `V14_S7_19_LOG.md` only (no source edits at STEP 0).

Commit message: `Stage 7.19: STEP 0 -- checkpoint + design decisions locked`

---

## STEPs 1-5 -- archive + rename pass (summary)

- STEP 1 (`25c1966`): pre-redesign baseline archived to `_archive/v14_customize_pre_redesign/customize.html` (4346-line snapshot, SHA256 `6d1d4a98...`).
- STEP 2 (`43e5ede`): 14 section header renames (General + Font unchanged per S2.1 table). 14/14 diff.
- STEP 3 (`4e2ed4c`): Spectrum Visualizer -- 9 control renames + section intro paragraph replaced + brand-name wall-of-text (SteelSeries Sonar / Voicemeeter) DELETED. 10/12 diff.
- STEP 4 (`04dcd17`): Card Shape (7 renames) + Track and artist text (12 renames). Title vs Artist disambiguated via input-id context. 19/19 diff.
- STEP 5 (`eb6fac3`): remaining 11 sections renamed (Outer glow, Text glow, Dynamic Colors / Auto-color, Spinning border, Album art, "Now Playing" label, Platform badge, Progress and time, Slide-in animation, Layout, General). 62/62 diff. Cumulative ~100 label rewrites across all rename STEPs.

---

## STEP 6 -- inline help text (high-impact sections)

Added `.control-help` CSS class to `<style>` block (var(--fs-caption), var(--text-secondary), 4-8px vertical margin, 1.5 line-height). Inserted `<p class="control-help">...</p>` paragraphs between rows in 4 sections.

| Section | Help paragraphs added | Notes |
|---|---:|---|
| Card appearance | 7 | One per control. Last row's existing inline-styled `.sec-help` REPLACED with new `.control-help`. |
| Audio bars (Spectrum) | 15 | One per control. 3 existing inline-styled `.sec-help` paragraphs (Reaction Speed / Smoothness / Frame rate) REPLACED with `.control-help`. Inline `<span>` hints next to autogain + mirror toggles LEFT IN PLACE (they're complementary inline cues, not full help). |
| Track and artist text | 12 | One per control. |
| Text glow | 5 (block-level) | **Pragmatic departure from "every control".** Each per-element block (NP / Platform / Title / Artist / Timestamps) has identical 3 controls (Enabled / Glow color / How strong the glow is); the labels are already self-explanatory after STEP 5 renames. Adding 15 row-level help paragraphs (3 per block × 5 blocks) would be repetitive noise. Instead: 5 block-level help paragraphs, one immediately after each sub-label, explaining what the block does for that element. Existing inline-styled `.sec-help` paragraph about the HUE-SHIFTED Artist Glow (line ~1108) left in place -- it's a cross-control note about Dynamic Colors interaction, not per-control help. STEP 7 may revisit. |

Total STEP 6: 39 help paragraphs added across 4 sections, 4 existing inline `.sec-help` paragraphs replaced with new `.control-help` style.

---

## STEP 7 -- inline help text (remaining sections)

Per S7.1 skip criteria: when the renamed label is already self-explanatory in < 5 words, skip help. When in doubt, include.

| Section | Help paragraphs added | Notes |
|---|---:|---|
| General | 1 | "Overall transparency" -- the slider value clarifies "100 = fully solid". |
| Layout | 0 | The section's `sec-help` intro paragraph + "Show / Lock Elements" sub-label + adjacent "Left toggle = visible. Right toggle (🔒) = lock" inline note + "Tip" trailing paragraph already provide comprehensive guidance. Per-row help would be redundant. |
| Font | 0 | The section's `sec-help` already explains "Typeface" comprehensively; the only control is self-explanatory after that. |
| Auto-color from album art | 0 | Existing `sec-help` block-level intro explains the section's behavior. The 16 per-element toggles are simply "auto-color this element from album art? yes/no" -- the label IS the help. Adding 16 redundant paragraphs would amplify the audit's "density without grouping" pain point. |
| Spinning border | 0 | Two controls (Enabled, How fast it spins) both self-explanatory. |
| Outer glow | 0 | Five controls (Enabled, First color, Second color, How strong the glow is, How fast it pulses) all self-explanatory after STEP 5 rename. |
| Album art | 1 | "Position" → "Left, right, or center of the card." (the inline `(double fade)` annotation in option text already clarifies the Center option's behavior). |
| "Now Playing" label | 1 | "What the label says" → common-text examples. Bars sub-block intro implicit from sub-label. |
| Platform badge | 1 | "Label for SoundCloud" → "Override the badge text when SoundCloud is the source." |
| Progress and time | 1 | "Enabled" → "Shows or hides the progress bar and time text." (the section's `sec-help` intro covers the rest). |
| Slide-in animation | 2 | "From which side" → direction clarification. "Easing curve (advanced)" → spring/ease/bounce/linear explanation. |

Total STEP 7: 7 help paragraphs added. Skip decisions documented; most remaining sections rely on their existing section-level `sec-help` intros + the post-STEP 5 friendly labels.


