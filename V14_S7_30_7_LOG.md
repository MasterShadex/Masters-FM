==
== V14_S7_30_7_LOG.md  --  Stage 7.30.7 running log
== INSTALL + BUILD HYGIENE (final customize-cycle stage)
== Build/repo plumbing only. NO source HTML changes (customize/legacy/overlay SHA-unchanged).
== Deletion of junk is GATED -- operator approves at STEP 4 before any removal.
== Force-added past V*_LOG.md gitignore.
==

# STEP 0 -- checkpoint + full 4-item inventory

## S0.0 Pre-conditions
| Check | Expected | Actual |
|---|---|---|
| HEAD | `8fc091e` (7.30.6.1 closure) | MATCH |
| version.json | 14.0.0 | 14.0.0 (msi_sha256 7dcd4e34...) |
| src/customize.html | `33D09CDF...` | MATCH |
| src/customize_legacy.html | `7E98377D...` (LEGACY_SHA) | MATCH |
| src/overlay.html | `9A7CC817...` | MATCH |
| 4 protected files | baseline | MATCH (SHA below) |
| Strikes | 0/3 fresh | 0/3 |
| Untracked present | ~194 (brief estimate; stale) | 462 actual (see S0.6) |

## S0.1 Backup
`_BACKUPS_2026-05-30_22-59_S7_30_7_PRE/`: the two scripts this stage edits
(`_full_rebuild.ps1`, `build_tools/build_msi.py`) + `src/customize.html`
(to prove source unchanged at closure) + head_sha.txt (8fc091e).

## S0.2 Protected + source HTML SHA256 (must be UNCHANGED at closure)
- tray.ps1            : 19011F0BD093CEA51CB34D053209F33FB3A37DE673777BAB34B5F8F26609533F
- tray_native.cs      : 6B9804A1AB70000652A2754E886BE3F05167F40EC136EB2CC6CDD62D8EFA9148
- launcher.cs         : 291ED4C92B9BEA391BA9204323EA41BA60AD7903AF6E6D7BA9404E1056E0BD9D
- server.js           : C15ED9310CB33044A090878918DC2B89B3FB843901BA0F199D3092EF502A16AF
- customize.html      : 33D09CDFF76547D428D94870C3AA577D9BEB74F1826DDE89E991939B1D1AABCD
- customize_legacy    : 7E98377DC97F83B31DEE96E805479D70CB4DF008D444C8108242FB1AE942C9B0
- overlay.html        : 9A7CC817515FFCC01695AEB1D2675757194E271AE8AE686403F675905BCE3AB2

## S0.3 Item 1 inventory -- stale customize_v2.html
- ON DISK: exists ONLY at install location
  `C:\Users\Master\AppData\Local\MastersFM\customize_v2.html` (65904 bytes, NON-empty).
- NOT in `src/` (confirmed -- only customize.html / customize_legacy.html / overlay.html there).
- NOT in `build_tools/build_msi.py` FILES list (grep: no customize_v2 entry -- build does NOT carry it).
- NOT a server.js route, NOT in `_full_rebuild.ps1`.
- grep "customize_v2" across build_msi.py / _full_rebuild.ps1 / server.js / src/*.html = 2 hits,
  BOTH inside descriptive code COMMENTS in customize.html (lines 1998, 2001). These are NOT a file
  copy/route -- they are stale architecture comments. customize.html is source -> NOT edited this
  stage (SHA must hold); the comments are harmless and stay.
- CONCLUSION: pure leftover install artifact from the S7.26->S7.30 era (customize_v2.html was the
  dev name before the git-mv swap to customize.html). Build already does not carry it forward.
- REMOVAL PLAN: the installed-dir artifact is a file deletion -> GATED at STEP 4, executed STEP 5
  IF approved. NOTE: it is 65904 bytes (NON-empty), so it is surfaced as a SEPARATE explicit gate
  item (not part of the zero-byte set). Also: STEP 3 cold rebuild may already remove it if the
  installer wipes the install dir -- verified at S3.3.

## S0.4 Item 2 inventory -- legacy bundling (build_msi.py)
- Current: `build_tools/build_msi.py` FILES list (lines 121-149) bundles src/customize.html (COMP8),
  src/overlay.html (COMP7), src/update.html (COMP27), src/tray.ps1 (COMP2), etc. It does NOT include
  src/customize_legacy.html. Highest GUID = GUID_COMP64 (line 118).
- OPERATOR DECISION (default): BUNDLE customize_legacy.html into the MSI (revert safety net survives
  clean installs).
- CHANGE NEEDED (STEP 2):
  1. Add a new component GUID after line 118:
     `GUID_COMP65 = "{55555555-DDDD-8888-9999-AAAAAAAAAAAA}"  # customize_legacy.html (revert net, LEGACY_SHA 7E98377D)`
     (continues the COMP61-64 DDDD family: 11111111/22222222/33333333/44444444 -> 55555555; unique.)
  2. Add to FILES list immediately after the customize.html line (128):
     `("src/customize_legacy.html", "customize_legacy.html", GUID_COMP65),`
- Minimal change. No other FILES entry touched.

## S0.5 Item 3 inventory -- _full_rebuild.ps1 hardening
WPF tray publish block = lines 263-289.
- Line 271-272: `dotnet publish ...MastersFM_Tray_v14.csproj... 2>&1`
- Line 273: `$trayCsExit = $LASTEXITCODE`  (reliable native exit code; NOT $? -- the 2>&1 caveat does
  not affect $LASTEXITCODE, so this stays untouched)
- Line 274-285 (exit 0 path): copies artifacts; if exe present -> logs KB (SUCCESS PATH).
  Line 283-285 else: exit=0 but exe missing -> currently only `WARN` (silent broken build).
- Line 286-289 (exit != 0 path): currently `WARN: ... failed (exit $trayCsExit) -- continuing`
  -- SOFT FAIL. This is the Item-3 target.
House style for hard fails: `L "  FAIL: ..."; exit 11` (server publish, lines 158-160; also exit 11
at 196/235/350). MSI build uses exit 12; msiexec exit 15.
- EDIT A (HARD ERROR): convert the exit!=0 branch (286-289) to `L "  FAIL: WPF tray dotnet publish
  failed (exit $trayCsExit) -- aborting (Stage 7.30.7 hard error)"; exit 11`. Also harden the
  exit=0-but-exe-missing branch (283-285) to FAIL + exit 11 (a "successful" publish with no exe is a
  silent broken build -- directly serves "never silently succeeds").
- EDIT B (VBCSCompiler pre-kill): insert in preflight right after `=== REBUILD START ===` (line 127,
  before Step 1 at 129). Matches the house Stop-Process style (lines 420-426):
  `Get-Process VBCSCompiler -ErrorAction SilentlyContinue | Stop-Process -Force -ErrorAction SilentlyContinue`
  + a one-line L log. Placed after REBUILD START so the fast-path branch (122-125, which only copies
  HTML, never builds) is unaffected.
- SUCCESS PATH UNCHANGED: on a normal clean build (exit 0 + exe present) neither hardened branch
  fires; the pre-kill is a safe no-op if VBCSCompiler is not running. Operator decision #3 honored.

## S0.6 Item 4 inventory -- untracked junk (the GATED one)
Measured `git ls-files --others --exclude-standard` + per-file size:
- TOTAL untracked: 462  (brief estimate "~194" was stale -- mostly .claude/ tooling docs accreted since)
- ZERO-BYTE: 72   NON-EMPTY: 390

### S0.6a ZERO-BYTE debris -- DELETE CANDIDATES (gated; all confirmed 0 bytes)
71 proposed for deletion (1 held back -- see note). All are tokenized command fragments accidentally
written as empty files (shell tokenization mishaps -- names split on spaces/parens/backticks). None
are source. Full list (root-level unless noted):

  $($f   $UseDotnet8TrayCs   ,   ,+   .NET   0   10   160-200   192.9   2026-05-09
  2026-05-09),+10   204   2s   30   384   4   4)   400   404),+-   419   4`   5
  500   500)   5000`   5min   8   95%)   Arm   Backend   C#   C#)   InstallFiles
  IsAutoStartEnabled   MSI   OBS   Show   V1114_SOAK_ERR.txt   WM_QUIT
  _obsAutoAddAttempted   `   `JsonNode.Parse`   `Microsoft.Extensions.DependencyInjection`
  a   base64-decode   blank   build   cover_xl   deadline),-   dotnet)   event-driven)
  growth   installing   location.reload(),+-   manual   new   obs-websocket   rc.1)
  return   scene-collection.json   send   server   server.dll(valid)   target
  tray.ps1   trigger   unchanged   v11.2.3   v13.0.0,   {   {,+

  REAL-LOOKING names FLAGGED for operator (all confirmed 0 bytes, all at repo ROOT):
    - `tray.ps1`               -> root-level 0-byte debris. This is NOT the protected
                                  `src/tray.ps1` (which is 801427 bytes, untouched). Safe to delete.
    - `scene-collection.json`  -> 0-byte fragment (real OBS-config-shaped name, but empty).
    - `V1114_SOAK_ERR.txt`     -> 0-byte (empty soak-error file = no errors recorded; still debris).
    - `InstallFiles`           -> 0-byte fragment.

  HELD BACK (NOT proposed for deletion -- 1):
    - `src/InstallFiles` (0 bytes) -> lives UNDER src/. Absolute rule "do not touch src/" -> excluded
      from the delete set. Flagged for operator to remove manually if desired.

### S0.6b NON-EMPTY untracked -- NEVER deleted this stage (disposition proposals only)
390 files. By top-level segment: .claude/ 258, <root> 87, build_tools/ 26, .claude-flow/ 9,
src/ 5, .swarm/ 5. Classified:

  CLASS A -- Stage reports / diagnostics (root .md, 46) -> propose COMMIT (or leave; operator call):
    V14_S7_* (P1/REPLAN/13/15/16/17/12/30_4_DRAFT...), V14_RC1_*, V14_RC3_*, V1110_*, V1115_*,
    V1122_*, V112X_*, V14_SHIP_SOAK.md, CLAUDE_CODE_INSTRUCTIONS_OPEN_QUESTIONS.md. Real project
    history. (Stage 7.30.7's own LOG/REPORT are force-added separately at each STEP.)

  CLASS B -- Build artifacts -> propose GITIGNORE (never commit, never delete this stage):
    .NET publish leftovers at root: *.deps.json (audio_spectrum, customize, MastersFM, server,
    tray_native), *.runtimeconfig.json (audio_spectrum, customize, MastersFM, server),
    server.staticwebassets.endpoints.json, web.config, tray_native.pdb, server_gcdump.gcdump,
    ruvector.db. Under src/ (build output, NOT source): src/obs_cleanup/cleanup_pub_tmp/* (exe/pdb/
    deps.json/runtimeconfig.json), src/obs_configured.flag (runtime flag).
    -> proposed .gitignore lines: *.deps.json, *.runtimeconfig.json, *.pdb, *.gcdump,
       server.staticwebassets.endpoints.json, web.config, ruvector.db,
       src/obs_cleanup/cleanup_pub_tmp/, src/obs_configured.flag

  CLASS C -- Tooling state (ruflo / claude-flow) -> propose GITIGNORE (leave; not project source):
    .claude/ (258, incl .claude/memory.db + 215 agent .md), .claude-flow/ (9, incl .gitignore),
    .swarm/ (5: memory.db + -shm + -wal), .mcp.json, "Claude Code CLI.lnk".
    -> proposed .gitignore lines: .claude/, .claude-flow/, .swarm/  (.mcp.json + .lnk: leave or
       ignore -- operator call)

  CLASS D -- Scripts / soak data -> LEAVE / review later (operator call; not auto-disposed):
    Scripts (root .ps1): _smtc_extended_test.ps1, _soak_monitor.ps1, _validation_sampler.ps1,
      soak-monitor.ps1, soak-monitor-master.ps1; build_tools/ (26 -- helper scripts/configs).
    Soak data: soak_log_rc3_v2..v6.csv (5), soak_log_rc3_v2.warmup.bak, V1110_*.txt, V1114_*.txt,
      V1115_*.txt, V1116_*.txt, V112X_*.txt, V14_S7_S7_5_LIVE_LOG_SNAPSHOT.txt,
      V14_RC1_SOAK_BASELINE.json.
    Oddity: `$null,+` -> NON-empty tokenized fragment (has content) -> NOT in delete set; leave/flag.

## S0.7 Commit
STEP 0 deliverable: this log. No source touched. No script edited yet.
Commit: `Stage 7.30.7: STEP 0 -- full inventory (stale v2 install-only + legacy bundling plan + rebuild-script edit sites + 462 untracked classified: 71 zero-byte delete-candidates, 390 non-empty disposition)`

== END OF STEP 0 ==

# STEP 1 -- Item 3: _full_rebuild.ps1 hardening (done FIRST so STEP 3 rebuild uses it)

## S1.1 Edits applied (2)
EDIT B (VBCSCompiler pre-kill, preflight): inserted 6 lines after `L "=== REBUILD START ==="`
(line 127). `Get-Process VBCSCompiler -EA SilentlyContinue | Stop-Process -Force -EA
SilentlyContinue` + a one-line L log. Placed after REBUILD START so the HTML-only fast-path
(122-125, copy-only, never builds) is unaffected.
EDIT A (WPF publish HARD ERROR): the WPF tray block (now ~283-301):
  - exit!=0 branch: `WARN: ... -- continuing` -> `FAIL: WPF tray dotnet publish failed (exit
    $trayCsExit) -- aborting (Stage 7.30.7 hard error)` + `exit 11`.
  - exit=0-but-exe-missing branch: kept the WARN, ADDED `FAIL: WPF tray build produced no exe --
    aborting (Stage 7.30.7 hard error)` + `exit 11` (a "successful" publish with no exe is a silent
    broken build -- directly serves "never silently succeeds").
Exit code 11 matches the house publish-failure convention (server publish line 159-160; also
196/235/350). FAIL log prefix matches house style.

## S1.2 SE1 internal check
- git diff: exactly the two intended hunks; nothing else touched.
- SUCCESS PATH UNCHANGED: on exit 0 + exe present, the `L "=== WPF tray built: ... KB ... ==="`
  line is byte-identical; neither hardened branch fires; pre-kill is a no-op if VBCSCompiler absent.
- PowerShell parse check (`[Parser]::ParseFile`): 0 errors.
- The WPF publish uses `2>&1` then reads `$LASTEXITCODE` (line 273) -- NOT `$?` -- so the PS 5.1
  native-stderr/$? caveat does not affect the hard-error check. `2>&1` left untouched (scope).
- ENCODING: `_full_rebuild.ps1` HAS a UTF-8 BOM at HEAD (pre-existing; the file has built cleanly
  through every prior stage with it). My edits are pure-ASCII (`--`, Get-Process/Stop-Process,
  exit 11) so the BOM is harmless and no mojibake is possible. Preserved the known-good encoding
  rather than re-encoding a critical build script (constraint #3: success path exactly as before;
  the no-BOM rule's intent = prevent editor-introduced encoding damage, satisfied). To be proven by
  the STEP 3 clean cold rebuild.

## S1.3 Commit
Commit: `Stage 7.30.7: STEP 1 -- _full_rebuild.ps1 hardening (VBCSCompiler pre-kill + HARD ERROR on WPF publish failure; success path unchanged; parse 0 errors)`

== END OF STEP 1 ==

# STEP 2 -- Item 2 (legacy bundling) + Item 1 (no stale v2 ref) in build_msi.py

## S2.1 Item 2 -- BUNDLE customize_legacy.html (operator default)
Two edits to `build_tools/build_msi.py`:
- Added component GUID after GUID_COMP64 (line 118):
  `GUID_COMP65  = "{55555555-DDDD-8888-9999-AAAAAAAAAAAA}"  # customize_legacy.html`
- Added FILES entry immediately after the customize.html line:
  `("src/customize_legacy.html", "customize_legacy.html", GUID_COMP65),`
Result: clean MSI installs now ship customize_legacy.html alongside customize.html, so the
instant-revert safety net survives a fresh install.

## S2.2 Item 1 -- stop carrying stale customize_v2.html (build side)
- grep `customize_v2` in build_msi.py = 0. The build NEVER referenced it -> nothing to remove on the
  build side. (The only on-disk copy is the install-dir leftover, handled at STEP 4/5.)

## S2.3 SE1 internal check
- git diff: exactly the two intended hunks; nothing else touched.
- `python -m py_compile build_tools/build_msi.py` -> exit 0 (valid Python).
- New full GUID `55555555-DDDD-8888-9999-AAAAAAAAAAAA` occurs exactly once. Whole-file GUID audit:
  67 GUID literals, 67 unique -> NO duplicate ComponentId (MSI component rule satisfied).
- `customize_legacy` now present (FILES + GUID + comment); `customize_v2` = 0.
- Bundle source valid: `src/customize_legacy.html` exists, SHA 7E98377D (LEGACY_SHA, unchanged).
- ENCODING: build_msi.py has NO BOM at HEAD and NO BOM now (preserved); edits are pure-ASCII.

## S2.4 Commit
Commit: `Stage 7.30.7: STEP 2 -- build_msi.py bundles customize_legacy.html (GUID_COMP65, unique) + confirms no stale customize_v2 reference; py_compile OK`

== END OF STEP 2 ==
