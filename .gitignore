# === SECRETS — NEVER COMMIT ===
*.cer
*.pfx
*.p12
*.pem
*.key
*.env
*.envrc
secret*
token*
credential*
*credential*
*password*

# === BUILD ARTIFACTS — COMPILED OUTPUTS ===
# Compiled C# binaries (regenerated by _full_rebuild.ps1)
audio_spectrum.exe
MastersFM.exe
MastersFM_Tray.exe
customize.exe
# server.exe built by pkg from server.js
server.exe
dist/
__pycache__/
# MakeCab / MSI artifacts
*.msi
*.cab
setup.rpt
setup.inf
# Old bootstrapper EXEs
Install Master's FM V*.exe
# Test/scratch EXEs from development sessions
_rcedit_test.exe
_test_noicon.exe
_test_server_new.exe
_test_server_orig.exe
_test_v2.exe
_test_verbose.exe
# .NET / NAudio / WebView2 DLLs (downloaded by _download_naudio.ps1 etc.)
*.dll

# === BUILD ARTIFACT FOLDERS ===
bin/
obj/
out/
build/
Release/
Debug/
# MSI build output folder (contains .msi + .cab — gitignored above; INSTALL.bat committed separately)
Master's FM Install/
# Desktop installer bundle (MSI + INSTALL.bat + .cer — all gitignored)
MastersFM_Installer/
# npm packages
node_modules/

# === BACKUPS (ALL OF THEM) ===
_BACKUPS_*/
*backup*/
*BAK*/
CHECKPOINT_*/

# === LOGS AND RUN-SPECIFIC DATA ===
*.log
logs/
V994_TRAY_LOG_TAIL.txt
V994_TRAY_SNAPSHOT.txt
_BACKUP_ROOT.txt
.v7_backup_ts

# === USER-SPECIFIC / MACHINE-SPECIFIC CONFIG ===
# User's saved preferences (colors, fonts, etc.) — machine-specific, not source
config.json
config_local.json
config_user.json

# === OS / TOOL DETRITUS ===
.vs/
.vscode/
*.user
*.suo
Thumbs.db
.DS_Store
desktop.ini

# === CLAUDE RUN-SPECIFIC WORKING FILES ===
# Session logs, final reports, perf/soak data — useful on disk but not in repo
V*_LOG.md
V*_FINAL_REPORT.md
V*_SOAK.csv
V*_BASELINE*.csv
V*_DIAG*.csv
PERF_LOG.md
PERF_FINAL_REPORT.md
PERF_AFTER.csv
PERF_BASELINE.csv
AUDIT_*.md
TRIAGE_*.md
V7_AUDIT.md
V7_BUILD_LOG.md
V7_FINAL_REPORT.md
V7_IDEAS.md
V7_LAYOUT_AUDIT.md
V7_LAYOUT_EDITOR.md
V910_GITHUB_LOG.md
CLAUDE_CODE_INSTRUCTIONS.md
prompts.txt
_scan_catches.csv
_v9_gauntlet_picks.json

# === STAGE 7.30.7: .NET publish artifacts + local runtime state ===
# dotnet publish leftovers copied to the repo root by _full_rebuild.ps1 (regenerated each build)
*.deps.json
*.runtimeconfig.json
*.pdb
*.gcdump
/server.staticwebassets.endpoints.json
/web.config
# obs_cleanup publish output under src/ (build output, NOT source)
src/obs_cleanup/cleanup_pub_tmp/
src/obs_configured.flag
# Local runtime databases (machine-specific, regenerated)
ruvector.db
.swarm/

# === REPO CLEANUP 2026-06 (dev notes, claude tooling, naudio extracts) ===
# Dev investigation logs / diagnoses / reports / release notes drafts
_archive/dev_notes/
V[0-9]*_*.md
V[0-9]*_*.txt
V[0-9]*_*.json
V11*.txt
V112X_*.md
V14_*.md
V14_*.txt
CLAUDE_CODE_INSTRUCTIONS*.md
RC3_TESTER_ANNOUNCEMENT.md
TESTER_ANNOUNCEMENT*.md
RELEASE_NOTES_v14.0.0-rc.*.md
BAD_*.txt
MISSING_RESOURCES.txt
# Claude Code dev tooling
.claude/
.claude-flow/
.mcp.json
# NAudio nupkg extractions (rehydrated via _download_naudio.ps1)
build_tools/naudio/
# Random root garbage
$null,+
*.lnk
# === REPO CLEANUP 2026-06 round 2 ===
# Build/test investigation artifacts that shouldn't be in the public repo
V14_S4_P1_BASELINE_CAPTURES/
evidence/
logs/
tests/*
!tests/HeadlessTester/
test-ps51-load.ps1
# Node.js orphan (project doesn't use pkg anymore; build is C# via _full_rebuild.ps1)
package.json
package-lock.json
