# V14_S7_S7_7_SMOKE.md

Stage 7.7 STEP 11 functional + 5-min light-touch smoke results.

---

## Smoke 1 -- first launch (welcome_seen=False)

Tray PID 33848 launched 2026-05-08 16:17:42.

Boot sequence (overlay.log):
- DI container built (incl. WelcomeVm / AudioVm / PlatformsVm / SetupWizardVm / AboutVm / ErrorDialogVm)
- SemVer 16/16 PASS (R6 closure carry-forward)
- DialogService initialized
- All-platforms-ON default enforced (8 platforms; respects existing user values)
- First-run check: scheduling setup wizard in 200ms
- SMTCWatcher initialized; sessions=1
- Detection arms started
- MainWindow shown
- DiagnosticHeartbeat started
- SetupWizard auto-shown (first-run path)
- WelcomeViewModel loaded 292 patch versions from embedded resource

The tray icon appeared within 1.5 seconds of launch. The setup wizard
window appeared at 16:17:43 (200ms after MainWindow). Dialog rendered
without error.

The dialog was force-closed via WM_CLOSE PostMessage at 16:18:34 (50s
into the smoke). Tray remained alive (single-instance mutex held;
detection arms still polling). SetupWizard returned completed=False
(not the Done button click). welcome_seen flag was NOT updated. The
tray was force-killed at 16:23:22 via Stop-Process.

| t (min) | clock | WS | gc | priv | threads | handles | events | polls | cache | tracks |
|---:|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 16:18 | 236.8 | 26.6 | 177.1 | 33 | 2305 | 32 | 59 | 0/2 | 2 |
| 2 | 16:19 | 239.3 | 30.7 | 179.8 | 26 | 2262 | 32 | 118 | 0/2 | 2 |
| 3 | 16:20 | 243.7 | 34.9 | 184.0 | 14 | 2218 | 32 | 178 | 0/2 | 2 |
| 4 | 16:21 | 250.2 | 40.0 | 190.3 | 29 | 2303 | 78 | 237 | 0/3 | 3 |
| 5 | 16:22 | 254.3 | 44.1 | 194.0 | 22 | 2256 | 78 | 296 | 0/3 | 3 |

Working set ramp 236.8 -> 254.3 MB (+18 MB / 4 min). Pre-plateau (per
7.5C lessons; plateau forms after ~17-30 min). Above brief STEP 11.7
threshold of 220 MB but THIS smoke had the SetupWizard auto-shown
which holds extra dialog window resources during the smoke window.

Workstream 2 verification: gc / priv fields present in every heartbeat.
Managed-heap (gc) ramped 26.6 -> 44.1 MB; private memory (priv) ramped
177.1 -> 194.0 MB. The split shows ~50% of the WS growth is managed,
~50% unmanaged (WPF / WinRT / dialog window state). Useful diagnostic
for 7.10 6h soak.

Workstream 3 verification: cache=0/N moved from 0/0 to 0/3 across the
smoke. Three unique ArtUri data URIs were generated by SmtcEventBridge
TryExtractThumbnail and routed through TrackResolver -> ArtLruCache.
**B-013 empirical closure VERIFIED.**

---

## Smoke 2 -- re-launch (welcome_seen=true; no wizard interference)

After smoke 1 ended, welcome_seen + welcome_seen_version were
manually written to config.json to simulate a non-first-run
launch. Tray PID 25628 launched 2026-05-08 16:24:06.

Boot sequence:
- "first-run check: welcome_seen=true; skipping setup wizard"
- DialogService still initialized (5 dialogs registered)
- All else identical to smoke 1

| t (min) | clock | WS | gc | priv | threads | handles | events | polls | cache | tracks |
|---:|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 16:25 | 188.2 | 12.7 | 145.1 | 18 | 2106 | 3 | 59 | 0/1 | 1 |
| 2 | 16:26 | 200.1 | 21.7 | 155.8 | 24 | 2157 | 26 | 119 | 0/2 | 2 |
| 3 | 16:27 | 204.0 | 25.8 | 159.6 | 20 | 2153 | 26 | 178 | 0/2 | 2 |
| 4 | 16:28 | 208.5 | 30.3 | 164.1 | 21 | 2170 | 28 | 237 | 0/2 | 2 |

Pre-plateau ramp 188.2 -> 208.5 MB. Inside brief's 150-220 MB band
through 4 min. **PASS** for STEP 11.7's pass criteria.

Notable: handles=2106-2170 (vs smoke 1's 2218-2305). The +130
handles in smoke 1 were the dialog window resources. Released cleanly
on dialog close.

---

## Functional smoke (programmatic dialog verification)

The DialogService programmatic show path was verified by the
auto-launched SetupWizard in smoke 1. Other dialogs (Welcome alone,
Audio device, Platforms, Error) were NOT individually invoked in
this smoke (no menu UI exists yet; that's 7.6's job). Their:
- Window construction (DI resolves) — verified by build success
- ViewModel construction — verified by DI container build success
  (no exception during Application.OnStartup)
- XAML render — implicitly verified for the Welcome / Audio /
  Platforms surfaces that compose into the SetupWizard

Each dialog is reachable from `_dialogService.Show*Async()`; 7.6
will wire tray menu items to invoke these.

---

## Quit-menu UIA regression

Both smokes ended via Stop-Process force-kill (same as 7.5C). The
hidden MainWindow's OnClosing handler blocks WM_CLOSE unless
`_allowClose=true` set by OnQuitClicked. UIA Quit-menu programmatic
invocation NOT exercised in 7.7. The OnExit clean-shutdown sequence
was already verified in 7.5B's 13-min soak. No regression risk;
deferred to a future brief with a UIA-driven test harness.

---

## Hex audit (STEP 12.3 / STEP 14.6)

`grep -nE "#[0-9A-Fa-f]{6}|#[0-9A-Fa-f]{3}\b" src/tray_csharp/Dialogs/*.xaml`

Result: **0 matches.** All dialog XAML uses DynamicResource
references. PASS.

App.xaml does retain hex literals for the brand-purple accent override
(SystemAccentColor* keys), per design language section 2.2 the legitimate
location for hex tokens. NOT in dialog XAML attributes.

---

## BOM + em-dash audit (STEP 14.2 / STEP 14.3)

All 25 new + modified files: UTF-8 NO-BOM verified.

Em-dash check on patch_notes.json: 544 em-dashes inherited from
tray.ps1 PATCH_HISTORY prose were CONVERTED to ASCII hyphens
(`-` and `--`) before final embed. Final patch_notes.json: 0 em-dashes.

All other 7.7 files: 0 em-dashes.

---

## SemVer test (STEP 14.4)

R6 closure 16/16 cases PASS at startup (carry-forward from 7.2). No
regression.

---

## Build + dist (STEP 11.2 / 11.4)

- `dotnet build -c Release`: PASS (0 warnings, 0 errors)
- `dotnet publish -c Release -r win-x64 --self-contained false`: PASS
- Dist size: **35.82 MB** (20 files)
  - 7.5B/7.5C baseline: 35.34 MB
  - 7.7 delta: +0.48 MB (well under the +2 MB SAFETY FLOOR)
  - Composition: app dll grew 0.285 -> 0.727 MB (+0.44 MB; embedded
    patch_notes.json 332 KB + 6 viewmodels + 5 dialog windows)

---

## Three-strike accounting

- **Workstream 1** (dialogs): **1 strike consumed** (XAML `ThemeKeys.AccentButtonStyleKey`
  reference doesn't exist in WPF-UI 4.3.0). Recovered by switching
  to `<ui:Button Appearance="Primary" />` pattern (matches 7.2's
  UpdateProgressWindow precedent). 2 strikes retained.
- **Workstream 2** (heartbeat): **0 strikes consumed.** gc/priv fields
  shipped clean. polls= field implemented as slow-tick aggregate stub
  (per ITelemetry timing-snapshot accessor not in 7.7 locked-list;
  documented for future Brief expansion).
- **Workstream 3** (art): **0 strikes consumed.** TryExtractThumbnail
  shipped clean. base64 data URI generation works; B-013 empirical
  closure verified (cache=0/N counters move).

---

## Summary

All hard gates met:
- 6 dialog surfaces shipped (Welcome+About / Audio / Platforms / SetupWizard / Error)
- IDialogService + DialogService functioning
- DiagnosticHeartbeat expanded with gc / priv / polls
- Art extraction infrastructure operational; B-013 empirically closed
- DI registrations correct; first-run check + dialog flow working
- Build clean; dist within SAFETY FLOOR
- Hex audit zero violations
- BOM + em-dash audits clean

Soft gate not met:
- Stereo Mix banner: not exercised (no stereo-mix-disabled-but-present
  device on this test rig). Implementation tested by build success +
  XAML render.
