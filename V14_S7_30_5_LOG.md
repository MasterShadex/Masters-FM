==
== V14_S7_30_5_LOG.md  --  Stage 7.30.5 running log
== customize.html: preset EXPORT + IMPORT (restore last 2 legacy preset features)
== Force-added past V*_LOG.md gitignore.
==

# STEP 0 -- checkpoint + research-first (legacy export/import extraction)

## S0.0 Pre-conditions

| Check | Expected | Actual |
|---|---|---|
| HEAD | `2f430ae` (Stage 7.30.4 closure) | MATCH |
| `git status` (tracked) | clean (version.json restored at 7.30.4) | clean (only transient .claude lock) |
| `src/customize.html` SHA256 | `5E59A262AC485537...` (7.30.4 closure) | MATCH |
| `src/customize_legacy.html` SHA256 | `7E98377DC97F83B3...` LEGACY_SHA | MATCH |
| `src/overlay.html` SHA256 | `9A7CC817515FFCC0...` | MATCH |
| version.json | 14.0.0 | restored at 7.30.4; unchanged |
| Strikes | 0 / 3 fresh | YES |

## S0.1 Disk backup

`_BACKUPS_2026-05-29_21-09_S7_30_5_PRE/`:
- `customize_pre.html` (184516 bytes; Stage 7.30.4 closure state)
- `head_sha.txt` (2f430ae)

## S0.2 Protected files SHA256 baseline (4)

| File | SHA256 |
|---|---|
| src/tray.ps1 | 19011F0BD093CEA51CB34D053209F33FB3A37DE673777BAB34B5F8F26609533F |
| src/tray_native/tray_native.cs | 6B9804A1AB70000652A2754E886BE3F05167F40EC136EB2CC6CDD62D8EFA9148 |
| src/launcher.cs | 291ED4C92B9BEA391BA9204323EA41BA60AD7903AF6E6D7BA9404E1056E0BD9D |
| src/server.js | C15ED9310CB33044A090878918DC2B89B3FB843901BA0F199D3092EF502A16AF |

(overlay.html `9A7CC817...`, customize_legacy.html `7E98377D...` baselined in S0.0.)

## S0.3 RESEARCH -- legacy pmExport (Item 1), customize_legacy.html line 5068

Legacy export design = **B: export a SAVED preset by name** (NOT current config).
- `pmExport(name)` fetches `GET /load-preset?name=<encoded>` -> nested config.
- Wraps in an ENVELOPE:
  - `format`: "mastersfm.preset"
  - `version`: 1
  - `name`: "<preset name>"
  - `exportedAt`: "<ISO timestamp>"
  - `exportedFrom`: "v9.6.3"
  - `config`: { ...nested config... }
- Filename: `MastersFM_<safeName>.json` where
  `safeName = name.replace(/[^a-zA-Z0-9 _\-]/g,'').trim() || 'preset'`.
- Download: `Blob([JSON.stringify(envelope, null, 2)], 'application/json')` ->
  `createObjectURL` -> `anchor.click()` -> `setTimeout(revokeObjectURL, 0)`.
- Client-side download, BUT makes 1 server call to fetch the saved preset first.
- Status via `pmStatus("Exported ...", true)`.

## S0.4 RESEARCH -- legacy pmImport (Item 2), customize_legacy.html line 5099

Hidden file input wiring (lines 5040-5050):
- `#pm-import-btn` (button) + `#pm-import-file` (`<input type=file>`).
- btn click -> `importFile.click()`; file change -> if `files.length`:
  `pmImport(files[0])`; then `importFile.value = ''` (reset for re-import).

`pmImport(file)`:
- `await file.text()` -> `JSON.parse` in try/catch -> on parse error:
  `pmStatus("File is not valid JSON", false)`; return.
- FORGIVING format detection:
  - envelope: `parsed.format === 'mastersfm.preset' && parsed.config`
    -> name = `parsed.name`, cfg = `parsed.config`.
  - bare object: cfg = `parsed`, name = filename (strip `.json` + `MastersFM_`).
  - else: `pmStatus("doesn't look like a preset", false)`; return.
- Sanitize name (regex, slice 64); de-conflict (" (2)", " (3)" if taken).
- Sanity check: `typeof cfg === 'object' && !Array.isArray(cfg)` else reject.
- Then **POST /save-preset {name, config}** -- legacy SAVES the import to the
  server (server deepMerges with DEFAULTS, so partial presets fill in). Then
  `pmStatus("Imported as X -- click Load to apply it")`. **Does NOT applyConfig
  directly.**
- Error handling: all failures via pmStatus; outer try/catch, no throw escapes.

## S0.5 LOCK -- v2 adaptation plan

**Legacy vs v2 divergence (documented; resolved by operator /goal v2-adaptation spec):**

| Aspect | Legacy (9.6.3) | v2 (this stage) | Source of truth |
|---|---|---|---|
| Export subject | saved preset by name | CURRENT live State.config | operator spec + gate ("matches current settings") |
| Export format | envelope (6 fields) | SAME envelope | legacy (mirror) |
| Export filename | MastersFM_<safeName>.json | SAME pattern | legacy (mirror) |
| Import mechanism | POST /save-preset (save, then Load) | direct apply: nestedToFlat -> State.config -> applyConfig + refreshControlValues | operator spec + gate ("controls revert, overlay updates") |
| Import format detect | envelope OR bare config | SAME (forgiving) | legacy (mirror) |
| Import validation | "is it an object" (loose; server deepMerges) | isValidPresetShape: non-null obj, not array, >=1 top-level key in FLAT_TO_NESTED_MAP values | operator spec (stricter -- import is the risk) |

Legacy informs FORMAT + forgiving-detection + filename; operator /goal spec sets
MECHANISM (current-config export, direct-apply import) + stricter validation.

**v2 EXPORT plan (Item 1):**
- "Export" button in `.preset-modal-footer`. Exports CURRENT config:
  `const nested = flatToNested(State.config)`; envelope `name = 'overlay-config'`
  (no preset name in current-config context); `exportedFrom = 'v14.0.0'`;
  filename `MastersFM_overlay-config.json` (safeName rule applied).
- Same Blob/anchor/revoke download as legacy. `pmStatus` on success/fail.

**v2 IMPORT plan (Item 2):**
- Hidden `<input type="file" id="preset-import-input" accept=".json,application/json" hidden>`
  + `#preset-import-btn` "Import" button in footer.
- `importPreset()`: `input.value=''`; `input.click()`.
- `handleImportFile(event)`: file -> FileReader.readAsText ->
  - `JSON.parse` in try/catch -> on error `pmStatus("not valid JSON", false)`, return.
  - forgiving detect: envelope (`format==='mastersfm.preset' && config`) ->
    candidate = `parsed.config`; else bare object -> candidate = `parsed`.
  - `isValidPresetShape(candidate)` (non-null obj, not array, >=1 key in
    `new Set(Object.values(FLAT_TO_NESTED_MAP).map(p => p[0]))`) -> else
    `pmStatus("doesn't look like a Master's FM preset", false)`, return (NO state change).
  - apply: `State.config = {...State.config, ...nestedToFlat(candidate)}` (MERGE
    so partial presets don't blank unset controls; pending verify nestedToFlat
    partial-input behavior in STEP 2) -> `applyConfig()` + `refreshControlValues()`
    -> `pmStatus("Imported + applied", true)`.
  - input change handler resets `input.value=''` so re-import of same file fires.
- VALIDATION GUARANTEE: malformed JSON, wrong-shape, empty file all caught BEFORE
  any State.config mutation. No unvalidated data ever reaches State.config.

**v2 anchors confirmed (customize.html):** State 1865, applyConfig 2017,
refreshControlValues 2030, flatToNested 2773, nestedToFlat 2811, pmStatus 3282,
renderPresetList 3295, savePreset 3359, openPresetManager 3406; modal footer 3550
(preset-name-input 3551 + preset-save-btn 3552); init binding block ~3519-3523.
No `APP_VERSION` const in v2 -> envelope `exportedFrom` hardcoded 'v14.0.0'.

## S0.6 Commit

STEP 0 deliverable: this log. No source touched.
Commit: `Stage 7.30.5: STEP 0 -- research-first (legacy pmExport + pmImport extracted + v2 adaptation locked)`

== END OF STEP 0 ==

# STEP 1 -- Item 1: Export (current config -> .json download)

Edits to src/customize.html (4, surgical):
- CSS `.preset-modal-actions` row (flex; padding below the save footer; `.btn` flex 1).
- `exportPreset()` (inserted after deletePreset): current `State.config` ->
  `flatToNested` -> legacy envelope `{format:'mastersfm.preset', version:1,
  name:'overlay-config', exportedAt:ISO, exportedFrom:'v14.0.0', config:nested}`
  -> Blob/anchor download `MastersFM_overlay-config.json` (legacy safeName regex)
  -> `setTimeout(revokeObjectURL,0)` -> pmStatus. Pure client-side (no server call).
- HTML: Export button (`#preset-export-btn`, `.btn .btn-ghost`) in `.preset-modal-actions`.
- Wired in the DOMContentLoaded modal block: click -> exportPreset.

SE1 internal check (preview MCP @ 127.0.0.1:8799 standalone; real exportPreset
called with Blob + URL + anchor.click stubbed to capture the would-be download):
- Page parses clean (title "Customize Overlay -- Master's FM"); console 0 warn / 0 error.
- exportPreset() no throw; captured Blob content = VALID JSON.
- Envelope shape MATCHES legacy: format / version / name / exportedAt / exportedFrom / config.
- flat -> nested mapping correct incl. c-border-colors comma-string -> array:
  `{"card":{"backgroundTop":"#ff00ff"},"border":{"colors":["#111111","#222222"]}}`.
- Status: "Exported current settings to MastersFM_overlay-config.json."

NOTE: 8765 standing-rule port was occupied by an unrelated server (chest-freezer
media); used 127.0.0.1:8799 (own python http.server on src/) for SE1 instead. Did
not kill the 8765 occupant (unknown owner).

Commit: `Stage 7.30.5: STEP 1 -- Item 1: preset export (client-side JSON download, mirrors legacy pmExport)`

== END OF STEP 1 ==
