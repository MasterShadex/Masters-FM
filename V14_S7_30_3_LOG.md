==
== V14_S7_30_3_LOG.md  --  Stage 7.30.3 running log
== customize.html DENSITY + CONTRAST pass (CSS-only)
== Force-added past V*_LOG.md gitignore.
==

# STEP 0 -- backup + read CSS + lock targets + baseline

## S0.0 Pre-conditions

| Check | Expected | Actual |
|---|---|---|
| HEAD | `e25e7fe` (Stage 7.30.2 closure -- REBUILD CYCLE COMPLETE) | MATCH |
| Protected files SHA256 (4 files) | match Stage 7.30.2 closure | all 4 MATCH (S0.2) |
| `src/customize.html` SHA256 | `AD7DABFC97AAB47E...` (V2 in production) | MATCH |
| `src/customize_legacy.html` SHA256 | `7E98377DC97F83B3...` LEGACY_SHA | MATCH |
| `src/overlay.html` SHA256 | `9A7CC817515F...` (Stage 7.20.6 baseline) | MATCH |
| `version.json` | `14.0.0` | MATCH |
| Strikes consumed | 0 / 3 fresh | YES |

All pre-conditions PASS.

## S0.1 Disk backup

`_BACKUPS_2026-05-27_15-21_S7_30_3_PRE/`:
- `customize_pre_density.html` (166358 bytes; Stage 7.30.2 closure)
- `head_sha.txt` (`e25e7fe`)

## S0.2 Protected files SHA256

| File | SHA (first 16) |
|---|---|
| `src/tray.ps1` | `19011F0BD093CEA5...` |
| `src/tray_native/tray_native.cs` | `6B9804A1AB700006...` |
| `src/launcher.cs` | `291ED4C92B9BEA39...` |
| `src/server.js` | `C15ED9310CB33044...` |
| `src/overlay.html` | `9A7CC817515FFCC0...` |
| `src/customize.html` (target) | `AD7DABFC97AAB47E...` |
| `src/customize_legacy.html` (must stay) | `7E98377DC97F83B3...` |

## S0.3 Current CSS state (what we're changing)

```css
.control-row {
    display: flex;
    flex-direction: column;
    gap: var(--s-2);             /* 8px label-input gap inside row */
    padding: var(--s-3);         /* 12px all sides; main density driver */
    border-radius: var(--r-md);
    margin-bottom: var(--s-1);   /* 4px between rows */
}
.control-row:hover {
    background: var(--c-surface-elevated);   /* #2e2e2e */
}
.supercat-body {
    padding: var(--s-2) 0;       /* 8px top/bottom */
}
```

Tokens in play:
- `--c-surface: #242424` (the supercat-body background)
- `--c-surface-elevated: #2e2e2e` (used by control-row hover + welcome/topbar)
- `--s-1: 4px`, `--s-2: 8px`, `--s-3: 12px`, `--s-4: 16px`

`.control-row` has NO background by default (transparent over the supercat-body
which sits on the sidebar). Labels (pure white per Stage 7.24) sit on the
sidebar's `--c-surface` = `#242424` -- that's the "white on grey contrast
weak" complaint.

## S0.4 Locked numeric targets (per brief)

| Property | Before | After | Delta |
|---|---:|---:|---:|
| `.control-row` padding | 12px (--s-3) | 8px (--s-2) | -4px (-33%) |
| `.control-row` margin-bottom | 4px (--s-1) | 2px (inline; new --s-half optional) | -2px (-50%) |
| `.supercat-body` padding | 8px (--s-2) | 4px (--s-1) | -4px (-50%) |
| `.control-row` background | transparent | new `--c-row-bg: #1c1c1c` | +contrast vs #242424 surface |

New token justification: `--c-row-bg` provides a darker row card so the
pure-white labels pop against a deeper grey than `--c-surface`. Applied to
`.control-row` only (surgical "Recommendation B" per brief) -- welcome
banner / search bar / top bar / supercat headers / preview status keep
their existing surface tokens, no global cascade risk.

`.control-row:hover` continues to use `--c-surface-elevated` -- since the
default row is now darker, hover-up to elevated remains a clear lift.

Slider thumb (16->14) and toggle (36x20->32x18) shrink: SKIP unless after
screenshots reveal need. Brief says optional.

## S0.5 Baseline measurement (browser MCP)

URL: `http://localhost:8765/customize.html` via preview MCP. localStorage
cleared so all 7 supercats expanded by first-load default.

| Metric | Value |
|---|---:|
| Total control-row count | 120 |
| Real scroll container | `div.sidebar-scroll` |
| `sidebar-scroll.scrollHeight` (px) | **10220** |
| Supercats expanded | 7 / 7 |
| Supercats collapsed | 0 |
| `.control-row` padding | 12px |
| `.control-row` margin-bottom | 4px |
| `.control-row` gap (internal) | 8px |
| `.control-row` background | rgba(0,0,0,0) (transparent) |
| `.supercat-body` padding | 8px 0px |

Note on `preview_screenshot`: tool has intermittently timed out across
the rebuild cycle (Stage 7.28, 7.30.2 same issue). Quantitative
`scrollHeight` measurement is the gate-critical evidence for density
delta; subjective contrast is operator's gate call. Inline browser MCP
state captured via `preview_eval`.

Evidence: `evidence/s7_30_3/baseline.json`.

## S0.6 Commit

STEP 0 deliverable: `V14_S7_30_3_LOG.md` + `evidence/s7_30_3/baseline.json`. No source touched.

Commit: `Stage 7.30.3: STEP 0 -- baseline + locked targets (control-row scrollHeight 10220px / padding 12 / margin 4 / supercat-body 8; targets 8 / 2 / 4 + new --c-row-bg #1c1c1c)`

== END OF STEP 0 ==

# STEPs 1+2 -- density + contrast (combined commit)

## S1+2.1 Edits applied to `src/customize.html`

```css
/* Token */
--c-row-bg: #1c1c1c;          /* NEW (Stage 7.30.3 surgical contrast) */

/* .supercat-body */
padding: var(--s-1) 0;        /* was var(--s-2) 0 -- 8 -> 4 */

/* .control-row */
padding: var(--s-2);          /* was var(--s-3) -- 12 -> 8 */
margin-bottom: 2px;           /* was var(--s-1) -- 4 -> 2 (inline 2px; --s-half not in token set) */
background: var(--c-row-bg);  /* NEW */

/* .control-row:hover unchanged -- still --c-surface-elevated */
```

## S1+2.2 Scope verification

Only `src/customize.html` touched. No JS, no SETTINGS_CONFIG, no
FLAT_TO_NESTED_MAP, no bind functions, no themes. Confirmed by
`git diff --stat` (16 inserts / 4 deletes; all in the CSS block).

`--c-row-bg` is the ONLY new addition to `:root` tokens. Justified by
brief: surgical contrast lift on the control row card without disturbing
welcome banner / search / top bar / supercat headers.

## S1+2.3 Commit

Commit: `Stage 7.30.3: STEPs 1+2 -- density tightening (.control-row padding 12->8, margin 4->2; .supercat-body 8->4) + surgical contrast (--c-row-bg #1c1c1c on .control-row only)`

== END OF STEPs 1+2 ==

# STEP 3 -- after measurement + delta

## S3.1 Browser MCP re-measure (same conditions as S0.5)

| Metric | Before | After | Delta |
|---|---:|---:|---:|
| `sidebar-scroll.scrollHeight` | 10220 px | **8964 px** | -1256 px (**-12.29%**) |
| `.control-row` padding | 12 px | 8 px | -4 px (-33%) |
| `.control-row` margin-bottom | 4 px | 2 px | -2 px (-50%) |
| `.control-row` gap (internal) | 8 px | 8 px | unchanged (label/input-group spacing kept) |
| `.control-row` background | rgba(0,0,0,0) | `rgb(28,28,28)` (#1c1c1c) | +explicit contrast vs supercat-body |
| `.supercat-body` padding | 8px 0px | 4px 0px | -4px (-50%) |
| Total `.control-row` count | 120 | 120 | unchanged |

Density delta is **-12.29%** of total sidebar scroll. Per-row footprint
drops ~10px (4 padding both sides + 2 margin -> 8 + 2 padding/margin), at
120 rows = ~1200px = matches measured delta.

## S3.2 Other surfaces (cascade-leak check)

| Surface | Background | Note |
|---|---|---|
| Welcome banner | `rgb(46, 46, 46)` = #2e2e2e | unchanged (--c-surface-elevated) |
| Top bar | `rgb(36, 36, 36)` = #242424 | unchanged (--c-surface) |
| Search bar | `rgba(0, 0, 0, 0)` | transparent / inherited; unchanged |
| Supercat header | `rgba(0, 0, 0, 0)` | transparent / inherited; unchanged |

NO CASCADE LEAK. `--c-row-bg` surgically applied to `.control-row` only.
All non-row surfaces preserve original tokens.

## S3.3 Commit

Commit: `Stage 7.30.3: STEP 3 -- after measurement (sidebarScroll 10220->8964px = -12.29%; other surfaces unchanged; no cascade leak)`
Evidence: `evidence/s7_30_3/after.json`.

== END OF STEP 3 ==

# STEP 4 -- round-trip smoke (CSS-only stage; verify JS untouched)

## S4.1 Stage 7.30.1 standing-rule self-test

`window.__roundTripSelfTest()` -> `{ ok: true }`. FLAT_TO_NESTED translator intact.

## S4.2 5 live-control spot checks

| # | id | type | value | nested path | OK |
|---|---|---|---|---|:---:|
| 1 | c-master-overall-size | slider | 70 | masters.overallSize | yes |
| 2 | c-card-top | colorPair | #112233 | card.backgroundTop | yes |
| 3 | c-master-glow | toggle | false | masters.glowEnabled | yes |
| 4 | c-font | select | Manrope | font | yes |
| 5 | c-spec-bars | slider | 60 | spectrum.barCount | yes |

5/5 PASS. Console clean (0 warnings, 0 errors). CSS-only changes did not
disturb the JS layer.

## S4.3 Commit

Commit: `Stage 7.30.3: STEP 4 -- round-trip smoke (self-test PASS + 5/5 spot checks; CSS-only stage doesn't disturb JS)`
Evidence: `evidence/s7_30_3/round_trip_smoke.json`.

== END OF STEP 4 ==

# STEP 5 -- fast-path file copy to install + SE2

## S5.1 Stage 7.25.5 fast-path semantics

The Stage 7.25.5 `_full_rebuild.ps1` fast path engages only when
`git diff --name-only HEAD` shows ONLY `src/customize.html` and/or
`src/overlay.html` in the diff. Since Stage 7.30.3 commits are committed
(working tree clean), the script's fast-path detection would return
false and fall through to cold rebuild.

For Stage 7.30.3 (HTML-only delta, no binary code, no MSI repackage
needed), the proper response is the documented fast-path file copy
directly:

```powershell
Copy-Item src/customize.html %LOCALAPPDATA%\MastersFM\customize.html -Force
```

This is exactly what `Invoke-HtmlOnlyFastPath` would do internally if the
working tree had un-committed changes.

## S5.2 Result

| Path | SHA256 |
|---|---|
| `src/customize.html` | `DBBE0A130241B0B3E17A6E1DC843CE8993523BA6A625AC53637C9A14A0B7459D` |
| `<install>\customize.html` | `DBBE0A130241B0B3E17A6E1DC843CE8993523BA6A625AC53637C9A14A0B7459D` |

Install SHA == source SHA. New density+contrast HTML live in install.

## S5.3 SE2 N/A

SE2 mandatory log inspection is for `_full_rebuild.ps1` invocations.
This stage used the fast-path file copy directly (Stage 7.25.5
mechanism); no rebuild log was generated. PyWebView re-reads
customize.html on the next open of the Customize Overlay window.

== END OF STEP 5 ==
