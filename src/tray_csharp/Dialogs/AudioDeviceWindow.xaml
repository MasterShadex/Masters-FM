<!-- Stage 7.7 Surface 05. Audio device dialog. WinRT-based enumeration
     via Windows.Devices.Enumeration; ASIO tab hidden (WinRT does not
     surface ASIO). All colors via DynamicResource. -->
<Window x:Class="MastersFM.Tray.Dialogs.AudioDeviceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Audio source"
        Width="640" Height="520"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        WindowStyle="None"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- INTERRUPT #3 STEP 3 (Issue 5): drag on chrome-less window. -->
        <Grid Grid.Row="0" Margin="0,0,0,8"
              MouseLeftButtonDown="OnTitleBarDrag">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Audio source"
                           FontSize="22" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock Margin="0,4,0,0"
                           Text="Pick the device Master's FM listens to for live spectrum visuals."
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap" />
            </StackPanel>
            <Button Grid.Column="1" Content="X" Width="32" Height="32"
                    Background="Transparent"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    BorderThickness="0"
                    Click="OnCloseButtonClick" />
        </Grid>

        <!-- Stereo Mix banner (visible when device exists but is disabled) -->
        <Border Grid.Row="1" Margin="0,0,0,12" Padding="12" CornerRadius="4"
                Background="{DynamicResource SystemFillColorAttentionBackgroundBrush}"
                BorderBrush="{DynamicResource SystemFillColorAttentionBrush}"
                BorderThickness="1">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding StereoMixDevicePresent}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding StereoMixEnabled}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Stereo Mix is disabled"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                    <TextBlock Margin="0,4,0,0"
                               Text="Enable Stereo Mix in Windows Sound to capture system audio output for the spectrum."
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </StackPanel>
                <Button Grid.Column="1" Content="Open Windows Sound"
                        Margin="12,0,0,0" MinWidth="160"
                        Command="{Binding OpenWindowsSoundCommand}" />
            </Grid>
        </Border>

        <!-- Tabs -->
        <TabControl Grid.Row="2"
                    Background="{DynamicResource SolidBackgroundFillColorTertiaryBrush}"
                    BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1">
            <TabItem Header="Output">
                <ListBox ItemsSource="{Binding OutputDevices}"
                         SelectedItem="{Binding SelectedDevice, Mode=TwoWay}"
                         Background="Transparent"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}"
                                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                    <TextBlock Text="{Binding Detail}" FontSize="11"
                                               Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                                </StackPanel>
                                <Border Grid.Column="1"
                                        Padding="6,2" CornerRadius="4"
                                        Background="{DynamicResource SystemAccentColorPrimaryBrush}"
                                        Visibility="{Binding IsDefault, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}">
                                    <TextBlock Text="Default" FontSize="10" FontWeight="SemiBold" Foreground="White" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="Input">
                <ListBox ItemsSource="{Binding InputDevices}"
                         Background="Transparent"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,4">
                                <TextBlock Text="{Binding Name}"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                <TextBlock Text="{Binding Detail}" FontSize="11"
                                           Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="ASIO"
                     Visibility="{Binding HasAsio, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}">
                <TextBlock Margin="12" Text="No ASIO devices found." />
            </TabItem>
        </TabControl>

        <!-- Status row -->
        <TextBlock Grid.Row="3" Margin="0,12,0,0"
                   Text="{Binding StatusText}"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <!-- Footer -->
        <Grid Grid.Row="4" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Refresh list"
                    MinWidth="120"
                    Command="{Binding RefreshAsync_CmdCommand}" />
            <Button Grid.Column="2" Content="Cancel" MinWidth="100" Margin="8,0"
                    Click="OnCancelClick" />
            <ui:Button Grid.Column="3" Content="Apply" MinWidth="100"
                       Appearance="Primary"
                       Click="OnApplyClick" />
        </Grid>
    </Grid>
</Window>
