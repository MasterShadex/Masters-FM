# Stage 7.13 -- Customize Overlay visual rebuild log

**Brief:** `CLAUDE_CODE_INSTRUCTIONS.md` (758 lines, mtime 2026-05-20 13:06)
**Scope:** Pure visual rebuild of `src/customize.html` to v14 design tokens. No JS/HTML structural changes. Single operator gate at STEP 10.
**Author:** Claude (autonomous run).
**Started:** 2026-05-20.

---

## STEP 0 -- Checkpoint + design-token inventory + customize.html structure map

### S0.0 Pre-conditions (verified)

| Check | Result |
|---|---|
| HEAD | `5b1249d` (memory.md: Stage 7.12 BATCH A/B/C closure checkpoint) |
| Working tree (source code) | Clean. Only `version.json` shows `msi_sha256` rewrite from the Batch C local rebuild -- expected, not a code change. |
| `src/customize.html` exists | Yes -- 4204 lines / 207199 bytes / mtime 2026-05-15 23:10 |
| 12 Stage 7.12 DIAG docs present | Yes (DIAG_01..DIAG_10 + DIAG_LOG + DIAG_SUMMARY) |
| 4 protected files unchanged | Yes (tray.ps1, tray_native.cs, launcher.cs, server.js -- SHA256 baseline matches `V14_S7_P1_PROTECTED_BASELINE.md`) |

### S0.1 Disk backup

Snapshot before any edits:

```
G:\Project Folder\Master FM\_BACKUPS_2026-05-20_13-08_S7_13_CUSTOMIZE_PRE\
  src_snapshot\         <- robocopy of src/ (exit code 1 = "copied files", success)
  customize_original\   <- customize.html + customize.cs + customize.csproj
  head_sha.txt          <- 5b1249d
```

This is the disk-only safety net. STEP 1 will additionally archive `customize.html` into the git-tracked `_archive/v12_customize_baseline/` directory.

### S0.2 v14 design-token inventory

See **`V14_S7_13_DESIGN_TOKENS.md`** (285 lines). It contains:

- Color palette (brand purple base/deep/glow, surface 0..3, text primary/secondary/tertiary, borders, semantic tag colors, accent-bar 3-stop gradient).
- Typography scale (Segoe UI / Cascadia Mono, 8 size/weight/line-height tokens).
- Spacing rhythm (4 / 8 / 12 / 16 / 24 / 32 px).
- Border-radius scale (card 12, button 8, input 6, pill 999, tiny 4).
- Animation durations + cubic-bezier easings approximating the WPF QuadraticEase / ElasticEase resources.
- CSS box-shadow approximations of the WPF DropShadowEffect resources.
- A ready-to-paste `:root { ... }` block for STEP 2, **including legacy aliases** (`--accent`, `--sidebar-bg`, `--text-muted`, `--border`, `--hover`, `--accent-hover`) so the existing class consumers do not need to be renamed.
- The 3px animated accent-bar HTML + CSS snippet, with the `prefers-reduced-motion` guard.

Sources of truth (all from `src/tray_csharp/Theme/`):

- `Colors.xaml` -- brushes & gradients
- `Typography.xaml` -- font family, weights, sizes, line heights
- `AppDialogStyle.xaml` -- corner radius, padding, drop shadow geometry
- `Animations.xaml` -- durations + easing functions
- `Buttons.xaml`, `Cards.xaml`, `Inputs.xaml` -- per-component radius/padding

### S0.3 customize.html structure inventory

**File geometry:**

| Region | Lines | Notes |
|---|---:|---|
| `<!DOCTYPE html><html><head>` | 1-3 | Standard preamble. |
| `<meta>`, favicon, manifest | 4-9 | No structural change needed. |
| Google Fonts CDN (Inter) | **10** | To be removed in STEP 3 (see S0.4 decision). |
| Inline `<style>` (main) | 11-543 | Single stylesheet. ~530 lines of rules. This is the primary STEP 2-8 rewrite target. |
| `</head>` | 544 | |
| `<body>` | 545 | |
| `<header class="topbar">` | 548-567 | Logo, status pill, Preset Manager, Reset, Apply. STEP 6 chrome. |
| `<div class="layout">` | 569 | Container for sidebar + preview. |
| `<aside class="sidebar">` | 572-... | Stack of `.section > .sec-header + .sec-body` collapsibles. STEP 5/6. |
| `<script>` (main JS) | 1554-3999 | **Do not edit.** ~2440 lines of preset wiring, theme grid, live-preview, layout editor. |
| Inline `<style>` (secondary -- lt-modal) | 4000-4202 | Layout-template modal styles. STEP 5/9 will retouch to match v14 tokens. |
| `</body>` | 4203 | |

**Sections (sidebar collapsibles, in DOM order):**

`themes`, `general`, `layout`, `font`, `card`, `dyncolors`, `border`, ... plus more below line 900 (album art, EQ bars, now playing text, platform badge, track title, artist, spectrum, progress bar -- each as its own `.section`).

Every section follows the same skeleton:

```html
<div class="section">
  <div class="sec-header" data-sec="<id>">
    <span class="sec-icon">...</span>
    <span class="sec-title">...</span>
    <span class="sec-chevron">v</span>
  </div>
  <div class="sec-body" id="s-<id>">
    <p class="sec-help">...</p>
    <div class="row">
      <div class="row-label">...</div>
      <div class="row-ctrl">...</div>
    </div>
    ...
  </div>
</div>
```

This regularity means **one set of styles** in STEP 5 will reskin every section. The HTML structure stays untouched.

**Existing CSS root variables** (lines 15-33 -- to be REPLACED in STEP 2 with the v14 token set + legacy aliases):

| Var | Old value | New (v14) | Strategy |
|---|---|---|---|
| `--bg` | `#08080f` | `#0A0A0F` (Surface0) | Replace value; same var name. |
| `--sidebar-bg` | `#0c0c18` | `var(--surface-1)` = `#14141B` | Alias -> new token. |
| `--panel` | `rgba(255,255,255,0.035)` | `var(--surface-2)` | Alias. |
| `--panel-hover` | `rgba(255,255,255,0.06)` | `var(--surface-2)` with lighter overlay | Replace. |
| `--border` | `rgba(140,70,255,0.18)` | `var(--border-subtle)` = `#1A1A24` | Alias. |
| `--accent` | `#9333ea` | `var(--brand-base)` = `#8B5CF6` | Alias -> brand-base. |
| `--accent-bright` | `#c084fc` | `var(--brand-glow)` = `#A78BFA` | Alias -> brand-glow. |
| `--text` | `#e2e8f0` | `var(--text-primary)` = `#F5F5F7` | Alias. |
| `--text-muted` | `#7b8db3` | `var(--text-tertiary)` = `#5C5C66` | Alias. |
| `--input-bg` | `rgba(255,255,255,0.055)` | `var(--surface-2)` = `#1F1F2A` | Alias. |
| `--input-border` | `rgba(140,70,255,0.22)` | `var(--border-subtle)` | Alias. |
| `--success` | `#22c55e` | `#22C55E` (TagNew) | Same hex, normalized. |
| `--error` | `#ef4444` | `#F87171` (TagRemoved) | Lift to v14 semantic. |
| `--sidebar-w` | `370px` | (unchanged) | Layout constant. |
| `--topbar-h` | `52px` | (unchanged) | Layout constant. |

**Counts (Grep confirmed):**

| Metric | Count |
|---|---:|
| CSS class selectors (`^\.[a-z...]`) | 139 |
| CSS id selectors (`^#[a-z...]`) | 9 |
| `@keyframes` rules | 1 (`dot-pulse` at line 337) |
| `@media` queries | **0** (STEP 7 will add `prefers-reduced-motion`) |
| Native input controls (range/color/checkbox/number/text) | 140 |
| `backdrop-filter` uses | 2 (topbar at line 48; lt-modal at 4004) |
| `box-shadow` uses | 8 (will be replaced with v14 shadow tokens) |

**JavaScript hook points** (do NOT edit -- listed only so STEP 8 / 10 can verify they still work):

| Function | Line | Purpose |
|---|---:|---|
| `buildThemeGrid()` | 1893 | Populates `#theme-grid` with theme cards. |
| `applyTheme(name)` | 1910 | Writes theme JSON into config + re-renders. |
| `scaleIframe()` | 1977 | Resizes the live-preview iframe responsively. |
| `rebuildLayoutEditor()` | 2057 | Rebuilds `.le-node` draggable nodes inside the preview. |
| `bindRange/Toggle/Select/Text/Color` | 2312-2333 | Wires DOM events -> config setters. |
| `syncRange/Toggle/Select/Text/Color` | 2352-2387 | Reads config -> writes DOM values. |

If these continue to work after STEP 2-9 then **the rebuild is by definition pure-visual**.

### S0.4 Decision: Inter vs Segoe UI

**Decision: Segoe UI** (drop the Inter Google Fonts CDN at line 10).

Rationale:

1. **Match WPF dialogs.** The native dialogs (`AudioDeviceWindow`, `AppDialogStyle`, etc.) all render in Segoe UI via the system. Using Segoe UI in customize.html makes the two surfaces visually cohesive instead of two different families.
2. **No network dependency.** customize.html is served by the local server (`server.js`) and opens inside the WebView2 host that ships with the MSI. Removing the Google Fonts CDN means the page does not block on a network fetch when the user is offline or when DNS for `fonts.googleapis.com` is slow.
3. **No layout regression.** Inter and Segoe UI have very similar metrics at 13-14 px body sizes. The 4 px spacing rhythm + line-heights in `V14_S7_13_DESIGN_TOKENS.md` were tuned against Segoe UI in WPF -- using the same family in HTML keeps vertical rhythm consistent.
4. **The font stack is already in the v14 tokens.** STEP 2 will inject `--font-ui: 'Segoe UI', system-ui, -apple-system, sans-serif;` directly into `:root`.

Side effects:

- Line 10 (`<link href="https://fonts.googleapis.com/...">`) is removed in STEP 3.
- Line 39 (`font-family: 'Inter', system-ui, sans-serif;`) becomes `font-family: var(--font-ui);` in STEP 3.
- The inline SVG at line 553 has `font-family="'Inter',sans-serif"` -- STEP 3 retargets this to `'Segoe UI',sans-serif` to keep the FM logo glyph consistent.

### S0.5 Commit

Commit message:

```
Stage 7.13: STEP 0 -- checkpoint + design token inventory + customize.html structure map
```

Files staged:

- `V14_S7_13_DESIGN_TOKENS.md` (new, 285 lines)
- `V14_S7_13_LOG.md` (this file, new)

**Not staged** (per brief): the `_BACKUPS_2026-05-20_13-08_S7_13_CUSTOMIZE_PRE/` directory. It is a disk-only safety net; the tracked archive happens in STEP 1 via `_archive/v12_customize_baseline/`.

**Not modified** (per brief):

- `src/customize.html` -- untouched in STEP 0 by design.
- `version.json` -- pre-existing modification from Batch C rebuild (only `msi_sha256` rewrite); intentionally left out of this commit.
- Any protected file (tray.ps1, tray_native.cs, launcher.cs, server.js).

---
