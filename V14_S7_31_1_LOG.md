==
== V14_S7_31_1_LOG.md  --  Stage 7.31.1 running log
== CERT-PINNING FIX: make auto-update work for friends (fixes 7.31 HARDEN-FIRST).
== ONLY src/tray_csharp/Update/UpdateCheckService.cs changes. NO real GitHub. NO version bump.
== Force-added past V*_LOG.md gitignore.
==

# STEP 0 -- research + pin choice + pseudocode (research-first)

## S0.0 Pre-conditions
| Check | Expected | Actual |
|---|---|---|
| HEAD | `cb7dd39` (7.31 closure) | MATCH |
| version.json | 14.0.0 | 14.0.0 (no bump this stage) |
| 4 protected + 3 source HTML SHA | baseline | MATCH (S0.2) |
| edit target | src/tray_csharp/Update/UpdateCheckService.cs | NOT protected (editable) |
| origin | github.com/MasterShadex/Masters-FM.git | untouched; NEVER push this stage |
| Strikes | 0/3 | 0/3 |

## S0.1 Backup
`_BACKUPS_2026-05-31_16-26_S7_31_1_PRE/UpdateCheckService.cs` (pre-fix).
Pre-fix SHA256 = CDECB33730FECCF95114A76F1DB3F03D4F6DF4D92B724F3DD54CBA0F2C37DD5D.

## S0.2 SHA256 baseline (must be UNCHANGED at closure; only UpdateCheckService.cs changes)
tray.ps1 19011F0B / tray_native.cs 6B9804A1 / launcher.cs 291ED4C9 / server.js C15ED931 /
customize.html 33D09CDF / customize_legacy.html 7E98377D / overlay.html 9A7CC817.
Protected list = tray.ps1 / tray_native.cs / launcher.cs / server.js -> UpdateCheckService.cs is
NOT among them (it lives in src/tray_csharp/Update/, editable WPF tray code). Confirmed.

## S0.3 Research -- current VerifyAuthenticode (the friend-blocker)
UpdateCheckService.cs:
  - const ExpectedCertCn = "MasterShadex" (line 21).
  - VerifyAuthenticode(msiPath) (lines ~385-419):
      (a) X509Certificate.CreateFromSignedFile(msiPath) -> X509Certificate2  (throws if unsigned)
      (b) chain.Build(cert) with RevocationMode=NoCheck -> REQUIRE true   <-- THE BUG: a self-signed
          cert only builds when it is in the verifier's Trusted Root; friends don't have it ->
          UntrustedRoot -> false -> reject. (7.31 Probe 1 proved this.)
      (c) subject.Contains("CN=MasterShadex") -> require.
  - Caller (DownloadAsync): on VerifyAuthenticode false -> delete .partial + TransitionTo(Available).
  - SHA256 GATE (gate 1, DownloadAsync lines ~262-277, runs BEFORE Authenticode): computes SHA256 of
    the download, compares to manifest msi_sha256; mismatch -> delete + Available. <-- the real
    file-integrity anchor; catches tampering + truncation + SHA-mismatch. STAYS UNCHANGED.

## S0.4 Pin value captured (from the real signed MSI; == CurrentUser\My signing cert)
  Subject    : CN=MasterShadex, O=MasterShadex
  Thumbprint : 4B1660FC0B77F55C7D47B3B9010C873E5CC2B2BF   (SHA1, 40 hex)
  PublicKey  : 
               8B7EF00ABE723D2ED3B27F0F368B010203010001   (540 hex; RSA-2048 SPKI)
  NotAfter   : 2031-04-21 (5y; _sign_msi.ps1 reuses this persistent cert across builds).

## S0.5 Pin choice = Pin-A (thumbprint + public-key equality) -- recommended by brief
RATIONALE: explicit, reviewable, machine-trust-INDEPENDENT, stricter identity than the old
CN-substring+must-be-trusted-root. Public-key compare added alongside thumbprint as defense vs a
(hard) SHA1-thumbprint collision.
PRESERVED (NOT loosened): SHA256 gate (real integrity anchor) UNCHANGED; failure-delete-revert
UNCHANGED (caller); downgrade + pre-release rejection (ProcessManifestJson) UNCHANGED; CN guard kept.
REMOVED: only the chain.Build machine-root dependency (the friend-blocker).
NOT "any valid signature": a wrong/different cert (thumbprint or pubkey mismatch) -> REJECT; unsigned
(CreateFromSignedFile throws) -> REJECT; tampered/SHA-mismatch -> REJECT at the SHA256 gate. Only our
EXACT cert + the operator's EXACT SHA256 (from the HTTPS manifest) is accepted.
MAINTENANCE NOTE (document in report): the pin is tied to the current cert. If the cert is ever
regenerated (expiry 2031, or deleted from the store so _sign_msi.ps1 makes a new one), the pinned
thumbprint + pubkey MUST be updated in UpdateCheckService.cs or auto-update will reject the new MSIs.

## S0.6 Pseudocode (STEP 1 target -- replaces the chain.Build block ONLY)
```
private const string PinnedThumbprint   = "4B1660FC0B77F55C7D47B3B9010C873E5CC2B2BF";
private const string PinnedPublicKeyHex = "3082...010203010001";  // full 540 hex

private bool VerifyAuthenticode(string msiPath, out string error):
    error = ""
    try:
        signer = new X509Certificate2(X509Certificate.CreateFromSignedFile(msiPath))  // unsigned -> throws -> reject
        if !signer.Thumbprint.Equals(PinnedThumbprint, OrdinalIgnoreCase):  error="thumbprint mismatch"; return false
        if !signer.GetPublicKeyString().Equals(PinnedPublicKeyHex, OrdinalIgnoreCase): error="pubkey mismatch"; return false
        if !signer.Subject.Contains("CN="+ExpectedCertCn, Ordinal): error="subject mismatch"; return false
        return true                       // pinned to OUR cert; NO chain.Build, NO Trusted-Root dependency
    catch (ex): error="exception: "+ex.Message; return false
```
Everything outside VerifyAuthenticode (SHA256 gate, download stream, state machine, install, manifest
logic) is byte-unchanged.

## S0.7 Commit
STEP 0 deliverable: this log (research only; no code change yet). No GitHub, no version bump.
Commit: `Stage 7.31.1: STEP 0 -- research VerifyAuthenticode + capture MasterShadex pin (thumbprint 4B1660FC + pubkey) + Pin-A design/pseudocode (preserves SHA256+revert+downgrade; removes only chain.Build machine-root dep)`

== END OF STEP 0 ==
