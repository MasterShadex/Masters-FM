==
== V14_S7_32_LOG.md  --  Stage 7.32 running log
== PATH A: keep V2 LOOK; bring back V1's full 17-category structure + every V1 setting;
== drop big inline help (welcome banner); tooltips-only help; import/export FULLY
== V1-COMPATIBLE + LOSSLESS (layout must round-trip; a friend's v12/V1 export must import).
== ONLY customize.html changes. customize_legacy.html READ-ONLY reference + revert net.
== Real GitHub never touched. No version bump (shipping stays 14.0.0).
==

# STEP 0 -- checkpoint + research V1 (the full spec) + plan

## S0.0 Pre-conditions
| Check | Expected | Actual |
|---|---|---|
| HEAD | 7.31.1 b372dab OR 7.31.2 HEAD | 500a7e0 (7.31.2 interim checkpoint HEAD) -- OK |
| git tracked source tree clean | yes | yes (only untracked debris + transient .claude lock; no M on tracked src) |
| version.json | 14.0.0 | 14.0.0 (BOM; shipping file, NOT my edit target) |
| 4 protected SHA | baseline | MATCH (S0.2) |
| customize_legacy.html | 7E98377D (LEGACY_SHA) | MATCH |
| overlay.html | 9A7CC817 | MATCH |
| customize.html (edit target) | 33D09CDF | MATCH |
| real MasterShadex/Masters-FM remote | UNTOUCHED | untouched (no push this stage) |
| Strikes | 0/3 | 0/3 |

## S0.1 Backup
`_BACKUPS_2026-06-02_20-07_S7_32_PRE/customize.html` (pre-restructure copy = revert net alongside legacy).

## S0.2 SHA256 baseline (only customize.html may change at closure)
tray.ps1 19011F0B / tray_native.cs 6B9804A1 / launcher.cs 291ED4C9 / server.js C15ED931 /
customize_legacy.html 7E98377D / overlay.html 9A7CC817 / customize.html 33D09CDF.

## S0.3 Research V1 (read-only, sourced from customize_legacy.html -- SE7: V1 is the spec)

### 1A -- Category structure: V1 has 17 collapsible sections (`<div class="section">` + `.sec-header[data-sec]`), in this order:
| # | data-sec | icon | title | legacy line |
|---|---|---|---|---|
| 1 | quick-settings | (lightning) | Quick Settings | 1125 |
| 2 | themes | (palette) | Themes | 1185 |
| 3 | general | (gear) | General | 1198 |
| 4 | layout | (ruler) | Layout (advanced) | 1218 |
| 5 | font | (abc) | Font | 1267 |
| 6 | card | (card) | Card appearance | 1295 |
| 7 | dyncolors | (palette) | Auto-color from album art | 1366 |
| 8 | border | (sparkle) | Spinning border (advanced) | 1461 |
| 9 | glow | (dizzy) | Outer glow | 1490 |
| 10 | textglow | (star) | Text glow | 1536 |
| 11 | art | (picture) | Album art | 1584 |
| 12 | np | (note) | "Now Playing" label | 1627 |
| 13 | platform | (tag) | Platform badge (SoundCloud, Spotify, etc.) | 1709 |
| 14 | text | (writing) | Track and artist text | 1776 |
| 15 | spectrum | (bar chart) | Audio bars | 1902 |
| 16 | progress | (stopwatch) | Progress bar and time | 2049 |
| 17 | anim | (clapper) | Slide-in animation (advanced) | 2130 |
(Exact emoji are carried verbatim into the rebuild; spelled here to honor the em-dash/ASCII log rule.)

### 1B -- Settings inventory per V1 category (V1 uses the SAME c-* IDs as V2; pickers `-p` omitted):
1. quick-settings: c-master-accent-color, c-master-overall-size, c-master-text-size, c-master-glow, c-master-animations (5)
2. themes: theme grid (buildThemeGrid; no c- input -- V2 equivalent is the c-theme dropdown)
3. general: c-overlay-opacity (1)
4. layout: c-layout-on, c-layout-grid, c-layout-vis-spectrum, c-layout-lock-spectrum (4)
5. font: c-font (1)
6. card: c-card-radius, c-card-thick, c-card-angle, c-card-top, c-card-bot, c-card-blur, c-card-opacity (7)
7. dyncolors: c-dynamic-colors + c-dyn-bg/glow/title/titleglow/artist/artistglow/np/platform/npglow/platformglow/spectrum/timestamps/tsglow/progressbar/border (16)
8. border: c-border-on, c-border-spd, c-border-colors (3)
9. glow: c-glow-on, c-glow-c1, c-glow-c2, c-glow-int, c-glow-pulse (5)
10. textglow: c-{np,platform,title,artist,ts}-glow-{on,color,size} (15)
11. art: c-art-on, c-art-position, c-art-w, c-art-fade (4)
12. np: c-np-text, c-np-size, c-np-color, c-np-spacing + c-bars-on, c-bars-color, c-bars-count, c-bars-speed + c-keep-on-pause (9)
13. platform: c-platform-on, c-platform-sc-label, c-platform-color, c-platform-dot, c-platform-size, c-platform-weight, c-platform-spacing (7)
14. text: c-title-{size,weight,color,mspd,mpause,spacing} + c-artist-{size,weight,color,spacing,mspd,mpause} (12)
15. spectrum: c-spec-on, c-spec-autogain, c-spec-sensitivity, c-spec-sensitivity-reset, c-spec-mode, c-spec-color, c-spec-mirror, c-spec-opacity, c-spec-bars, c-spec-gap, c-spec-radius, c-spec-min, c-spec-height, c-spec-response, c-spec-smooth, c-spec-fps (16)
16. progress: c-prog-on, c-prog-h, c-prog-radius, c-prog-track, c-prog-f0, c-prog-f1, c-prog-f2, c-ts-size, c-ts-color (9)
17. anim: c-anim-dur, c-anim-slide, c-anim-dir, c-anim-easing, c-anim-crossfade (5)
V1 distinct c-* controls = 119 (+ theme grid).

### 1C -- The GAP (V1 settings missing from V2's SETTINGS_CONFIG): **NONE.**
V2 SETTINGS_CONFIG = 120 entries. Every one of V1's 119 c-* controls is present in V2 (verified id-by-id;
V2 uses identical IDs). V2 adds exactly ONE extra: `c-theme` (a dropdown that replaces V1's theme button
grid -- functionally equivalent, not a dropped setting). So 119 (V1) + 1 (c-theme) = 120 (V2). V2 is a
strict SUPERSET. This is PURELY a re-categorization problem (V1's 17 sections were collapsed into V2's 6
supercats). **=> Item 3 (restore dropped settings) = SKIP. No SETTINGS_CONFIG / FLAT_TO_NESTED_MAP additions.**

V1->V2 supercat mapping that flattened the 17 (the thing to reverse):
- start  = quick-settings + general
- look   = themes + font + card + art
- text   = np(text part) + title + artist + progress
- effects= glow + textglow + dyncolors
- audio  = np(bars part) + spectrum  (+ c-keep-on-pause)
- layout = platform + border + layout + anim

### 1D -- V1 import/export FORMAT (THE compatibility target; legacy lines 5053-5154):
ENVELOPE (pmExport, legacy 5074-5081):
```
{ "format":"mastersfm.preset", "version":1, "name":"<preset>",
  "exportedAt":"<ISO>", "exportedFrom":"v9.6.3", "config": <FULL NESTED CONFIG> }
```
- `config` = the COMPLETE nested overlay config (deepMerge(DEFAULTS, S)) -- font, masters, card, border,
  glow, art, nowPlaying, bars, platformBadge, title, artist, spectrum, progressBar, timestamps,
  showAnimation, pauseBehavior, dynamicColors, overlay, lastPresetName, AND **layout {enabled, canvas,
  gridSize, nodes{...8 nodes with x/y/w/h/z/visible/locked/anchor}}**. Full DEFAULTS shape at legacy 2236-2295.
IMPORT (pmImport, legacy 5099-5154) -- FORGIVING:
- JSON.parse; if `parsed.format==='mastersfm.preset' && parsed.config` -> use parsed.config (envelope);
  else if object (not array) -> treat whole JSON as the config (bare config, filename = name);
  else reject. Shallow object check only (server deepMerge fills missing keys).
This is what a friend's v12/V1 export looks like. The new import MUST accept it; the new export already
matches the envelope.

CURRENT V2 (the bug):
- exportPreset (3452): `config = flatToNested(State.config)`. State.config is FLAT (c-* only) ->
  flatToNested only emits the 118 mapped paths -> **layout.canvas + layout.nodes geometry is NOT emitted
  (only the 4 layout flats) -> export is LOSSY for layout.**
- handleImportFile (3507): `State.config = assign(State.config, nestedToFlat(candidate))`. nestedToFlat
  pulls only the 118 mapped paths -> **layout.canvas/nodes in the imported file are DROPPED.**
- loadConfig (1904): `State.config = nestedToFlat(nested)` -> the full server nested (incl layout) is
  reduced to flat at load too. saveConfig/previewConfig (1936/1980) post `flatToNested(State.config)` ->
  the overlay only ever receives the flat-derived subset; unmapped keys (layout geometry, lastPresetName,
  liveAudioVisualizer, etc.) are silently lost. (The 2858 comment "pass through via deepMerge upstream"
  is STALE -- there is no such deepMerge today.)
=> ROOT CAUSE of the layout breakage = V2's whole config pipeline is flat-only; anything not in
   FLAT_TO_NESTED_MAP cannot round-trip. THIS is what Item 4 fixes.

### 1E -- Big text to strip (in customize.html, the edit target):
- Welcome banner: lines 1034-1041 (`<div class="welcome-banner" id="welcome-banner" hidden>` with the
  (wave) emoji, h3 "New to Master's FM customize?", p "Pick a theme below...", dismiss button).
- Supporting code to remove with it: `.welcome-banner` CSS (~448-460), initWelcome()/showWelcome()/
  hideWelcome() (1832-1861), cacheElements() welcome refs (1818-1821), the init() initWelcome() call (3576),
  WELCOME_SEEN_KEY const, $welcomeBanner/$welcomeDismiss/$showWelcomeAgain lets.
- Per-section help paragraphs: V2 has NONE (it is data-driven via renderControl + the 7.30.6 tooltip
  engine). So the only "big text" in V2 is the welcome banner. (V1's `<p class="sec-help">` + per-control
  `<p class="control-help">` never existed in V2.)

## S0.4 Plan + baseline counts
Baseline (must reconcile post-change): SETTINGS_CONFIG=120, FLAT_TO_NESTED_MAP=118 (2 intentionally
unmapped: c-theme, c-spec-sensitivity-reset), THEMES=22.

- **STEP 1 (Item 2 -- categories):** Add a `CATEGORIES` array (17 entries: {key,icon,label} in 1A order)
  + add a `cat:'<data-sec>'` field to all 120 SETTINGS_CONFIG entries (grouping only; tokens/types/help
  untouched). Replace the 6 static `.supercat` HTML blocks with one container that renderAllControls fills:
  for each CATEGORY build a `.supercat`/`.supercat-header`(icon+label+chevron)/`.supercat-body#supercat-<key>-body`
  using the EXISTING V2 classes (V2 look kept), then append each control to its `cat` body. Collapse JS
  (initSupercatCollapse) + search filter operate on `.supercat`/`.control-row` generically -> keep working.
  Expected counts unchanged (120/118/22); CATEGORIES=17 new.
- **STEP 2 (Item 3 -- restore dropped):** SKIP (1C = no gap). Note in log + commit "SKIP: no gap".
- **STEP 3 (Item 4 -- import/export lossless, THE crux):** Add `State.nested` (full nested passthrough)
  + a `deepMerge(base, over)` helper (plain objects recurse; arrays + primitives REPLACE). Wire:
  loadConfig -> keep `State.nested = clone(nested)`; export -> `config = deepMerge(State.nested,
  flatToNested(State.config))` (full + control overrides); import -> `State.nested =
  deepMerge(State.nested, candidate)` then flat update + applyConfig + previewConfig; saveConfig +
  previewConfig -> post `deepMerge(State.nested, flatToNested(State.config))` (overlay now receives the
  COMPLETE nested incl layout -> contract preserved + richer). Keep isValidPresetShape (accepts envelope
  or bare; rejects non-object/array/empty/wrong-shape). Result: layout.canvas/nodes + every unmapped key
  round-trips; a friend's V1 envelope imports + applies.
- **STEP 4 (Item 5 -- strip big text):** Remove the welcome banner block + its CSS + JS (1E). Tooltips
  (95/120 controls already have `help`, rendered as info-icons by renderControl) stay as the only help
  surface; no control loses help (re-categorization keeps each entry's `help` field).
- **STEP 5:** window.__roundTripSelfTest + 10 spot-checks (incl a layout-bearing preset) + cold rebuild
  (WebView2 lesson) + SE2 + overlay-live verify + version.json restore.

## S0.5 Commit (log only -- no code change yet, no GitHub write, no version bump)
Commit: `Stage 7.32: STEP 0 -- research V1 (17 categories + settings inventory + GAP=none + import/export format + big-text map) + plan`

== END OF STEP 0 ==

# STEP 1 -- restructure categories to V1's 17 (Item 2; V2 look kept)

## S1.1 Edits (customize.html only)
1. CSS: added `.supercat-icon.is-emoji` (centered emoji in the existing 18px icon box). The
   `.supercat` card + collapse styling is UNCHANGED (V2 look kept).
2. JS: added `CATEGORIES` (17 entries: key + emoji icon + label, order/names/icons VERBATIM from
   legacy 1A) + `CAT_OF` (120 control-id -> category map, sourced id-by-id from legacy 1B).
3. JS: rewrote `renderAllControls()` -- builds one `.supercat` shell per category into
   `#categories-root` (header = emoji icon span + label span + the SAME chevron SVG; body =
   `supercat-<key>-body`), then groups each control by `CAT_OF[id]`. (Was: 6 static containers keyed
   by `cfg.supercat`.)
4. JS: `initSupercatCollapse()` first-load default now collapses all categories EXCEPT quick-settings
   (mirrors V1's tidy sidebar + avoids a 17-section all-expanded scroll; saved '0'/'1' still win).
5. HTML: replaced the 6 static `.supercat` blocks with `<div id="categories-root"></div>`.
SETTINGS_CONFIG tokens/types/help/`supercat` fields UNCHANGED (the inert `supercat` field is left in
place; grouping authority is CAT_OF). No setting added/removed (GAP=none).

## S1.2 SE1 verification (DOM-free; build_tools/_s732_verify.js)
- ENCODING: customize.html no BOM (`3c 21 44` = `<!D`); em-dash count 0.
- No stale refs: `supercat-{start,look,effects,audio}-body`, `cfg.supercat`, `containers[` = NONE.
- PARSE: the single inline `<script>` block compiles via new Function() -> NO syntax error from the
  edits (a syntax error would blank the panel + spam console; none).
- INVARIANTS (all PASS): SETTINGS_CONFIG=120 (unique); CATEGORIES=17; CAT_OF maps 120; every control
  has a category; no orphan CAT_OF keys; every CAT_OF value is a real category; every category has
  >=1 control; counts sum to 120.
- Per-category counts (V1 order) match the 1B inventory exactly:
  quick-settings 5 | themes 1 | general 1 | layout 4 | font 1 | card 7 | dyncolors 16 | border 3 |
  glow 5 | textglow 15 | art 4 | np 9 | platform 7 | text 12 | spectrum 16 | progress 9 | anim 5 = 120.
- Browser render check via static server (port 8765 up, HTTP 200): DEFERRED. 3 Chrome extensions are
  paired -> any browser action forces an interactive browser-picker prompt, which would stall the
  autonomous run (operator rule: don't stop / don't ask mid-run). The node parse+logic check above
  proves the SE1 claims (categories == V1's 17; controls land in the right category; JS clean ->
  no load-time console error). Visual confirmation is folded into STEP 5 (cold-rebuild install render)
  + the STEP 6 operator gate. (node v24.14.1 present; tools.md lists it unchecked -- operator may add.)

## S1.3 Commit
Commit: `Stage 7.32: STEP 1 -- categories restructured to V1's 17 (V2 look kept); CATEGORIES+CAT_OF data-driven; SETTINGS_CONFIG tokens unchanged; SE1 parse+grouping invariants PASS`

== END OF STEP 1 ==

# STEP 2 -- restore dropped settings (Item 3) -- SKIP

GAP = NONE (log 1C). V2's SETTINGS_CONFIG (120) is a strict superset of V1's 119 c-* controls
(+ c-theme dropdown replacing V1's theme grid). No V1 setting is missing; this stage is purely
re-categorization (done in STEP 1). No additions to SETTINGS_CONFIG or FLAT_TO_NESTED_MAP.
Counts remain SETTINGS_CONFIG=120 / FLAT_TO_NESTED_MAP=118 / THEMES=22. No commit (no code change).

== END OF STEP 2 (SKIP) ==

# STEP 3 -- import/export V1-compatible + lossless (Item 4; THE crux)

## S3.1 Edits (customize.html only) -- the flat-only pipeline made lossless
Root cause (1D): State.config is FLAT; flatToNested/nestedToFlat only carry the 118 mapped paths, so
layout.canvas/nodes geometry (+ lastPresetName etc.) were dropped on load/save/export/import. Fix =
a full-nested passthrough + deep-merge:
1. `State.nested = {}` added (the last full nested config, from server load OR import).
2. `deepMerge(base, over)` helper added (plain objects recurse; arrays + primitives REPLACE; deep-clones;
   never mutates inputs). JSON-safe config.
3. loadConfig: `State.nested = deepMerge({}, nested)` retains the full server config (+ nestedToFlat for controls).
4. saveConfig + previewConfig: POST `deepMerge(State.nested, flatToNested(State.config))` -> overlay
   receives the COMPLETE nested (incl layout geometry) -> contract preserved + richer (fixes latent
   layout-truncation on Apply-to-OBS too).
5. exportPreset: `config = deepMerge(State.nested, flatToNested(State.config))` -> full V1-style envelope.
6. handleImportFile: `State.nested = deepMerge(State.nested, candidate)` FIRST (captures layout geometry +
   unmapped keys), THEN flat-control update -> applyConfig + previewConfig push the imported layout.
Envelope (format/version/name/exportedAt/exportedFrom/config) + forgiving import (envelope OR bare;
isValidPresetShape rejects non-object/array/empty/wrong-shape) UNCHANGED -> already V1-compatible.
7 wiring sites confirmed (load 1948, save 1970, preview 2016, deepMerge 2904, export 3557, import 3643).

## S3.2 SE1 -- DECISIVE round-trips (DOM-free; build_tools/_s732_roundtrip.js extracts the REAL
functions from customize.html and simulates the pipeline). ALL PASS:
- T1 new-panel export -> import == LOSSLESS incl LAYOUT: export carries layout.nodes geometry +
  enabled/canvas + unmapped lastPresetName; export1 deep-equals export2; layout subtree identical.
- T2 a reconstructed V1-format (friend v9.6.3/v12) envelope imports + applies: layout enabled/canvas/
  gridSize + albumArt + spectrum nodes applied exactly (incl locked:true); card/title/spectrum/border/
  font applied; flat controls updated; baseline-only keys (trackTitle node, progressBar, masters)
  preserved not blanked (V1 "fill missing from baseline" philosophy).
- T3 malformed/wrong-shape REJECTED: null / [] / {} / {no valid key} / string / number -> false;
  {card:...} and {layout:...} -> accepted.
NOTE: first T2 run flagged 1 assertion (expected whole-layout exact-equal); that was a TOO-STRICT TEST
(deepMerge correctly preserves baseline-only nodes; real V1 exports are complete -> T1's exact
round-trip is the full case). Corrected the assertion; NO customize.html change. NOT a strike (test
refinement, caught pre-commit). Re-run: ALL PASS.

## S3.3 Evidence
`evidence/s7_32/import_export_compat.json` (T1 lossless+layout, T2 V1-format import+apply, T3 rejects).

## S3.4 Commit
Commit: `Stage 7.32: STEP 3 -- import/export V1-compatible + lossless (State.nested passthrough + deepMerge; layout round-trips; friend's V1 export imports); decisive round-trips PASS`

== END OF STEP 3 ==

# STEP 4 -- strip big text; tooltips-only help (Item 5)

## S4.1 Edits (customize.html only) -- the ONLY big text in V2 was the welcome banner (1E)
Removed completely:
- CSS: the `.welcome-banner / .welcome-icon / .welcome-body / .welcome-dismiss` rule block.
- HTML: the welcome banner div (wave emoji + "New to Master's FM customize?" h3 + paragraph + dismiss
  button) AND the footer "Show welcome message" reshow link.
- JS: `WELCOME_SEEN_KEY` const; `$welcomeBanner/$welcomeDismiss/$showWelcomeAgain` refs; the 3 cache
  assignments; `showWelcome() / hideWelcome() / initWelcome()`; the `initWelcome()` call in init().
  cacheElements() kept as an empty stub (still called by init()).
- HTML: removed the now-childless `.sidebar-footer` div (it has a border-top + padding -> an empty one
  would render a stray bar). The `.sidebar-footer` / `.footer-link` CSS is left inert (harmless).
NOTE: V2 had NO per-section help paragraphs (it is data-driven; V1's `<p class="sec-help">` never
existed in V2). So the welcome banner was the entire "big text" surface.

## S4.2 SE1 verification
- No LIVE welcome references remain (only 3 removal-note comments). Grep for initWelcome/showWelcome/
  hideWelcome/welcome-banner/welcome-dismiss/show-welcome-again/WELCOME_SEEN_KEY -> only comments.
- Script compiles (new Function parse) + all category invariants still PASS.
- Tooltips intact = the ONLY help surface: help-icon engine (createHelpIcon + initHelpTooltips + CSS)
  present; 95 SETTINGS_CONFIG `help:` fields preserved (unchanged by recategorization). The 25
  controls without a tooltip remain the self-explanatory set classified in 7.30.6.1 (toggles / color
  pickers / reset button) -- deliberate, not a regression. GAP=none -> no restored control lacks one.
- em-dash 0; no BOM. Net diff -119 lines.

## S4.3 Commit
Commit: `Stage 7.32: STEP 4 -- big text removed (welcome banner CSS/HTML/JS + reshow link + empty footer); tooltips are the only help surface (95 help fields preserved)`

== END OF STEP 4 ==

# STEP 5 -- round-trip smoke + cold rebuild + overlay live

## S5.1 Round-trip self-test + spot-checks (bindings intact across the restructure)
- Round-trip self-test: covered by build_tools/_s732_roundtrip.js (STEP 3), which exercises the REAL
  flatToNested <-> nestedToFlat + deepMerge across all 8 control types (colorPair c-card-top, slider
  c-card-radius, toggle c-master-glow, colorList c-border-colors, select c-font, indexed-array
  c-prog-f0/f1/f2, plus the masters sentinel) -> identity holds; window.__roundTripSelfTest uses the
  same two functions.
- Bindings intact across the 17-category restructure: bindControl() dispatches on config.id +
  config.type (line 2589); refreshControlValues()/applyConfig() iterate SETTINGS_CONFIG by config.id
  (1948/1961). NONE of the binding/apply code reads category/supercat -> recategorization (renderAllControls
  + CAT_OF, the ONLY category-coupled code) cannot break a binding. Node verifier: 120/120 controls
  render into a category; all 8 types present.
- customize.html post-STEP4 SHA256 = 6DCA6F9A (pre-rebuild).

## S5.2 Cold rebuild + SE2 + install sanity (in progress)
Cold rebuild `_full_rebuild.ps1 -FullRebuild` -> log `_rebuild_logs/s7_32_cold_rebuild.log` (exit 0).
SE2 (UTF-16-aware scan of all 111 lines): CLEAN.
  REBUILD START -> VBCSCompiler pre-killed -> server.exe OK (net8.0 ASP.NET R2R) -> signed ->
  WPF tray built (MastersFM_Tray_v14.exe 160 KB) -> MSI signed (MasterShadex, Valid) -> uninstall=0 ->
  install OK -> MastersFM.exe launched -> REBUILD DONE OK. Zero error/warn/fail/exception/MSB/CSxxxx.
Install sanity:
  - installed C:\Users\Master\AppData\Local\MastersFM\customize.html SHA256 = 6DCA6F9A == src edit
    (the 17-category + lossless-import/export + tooltips-only build is the one now installed).
  - tray (MastersFM PID 26476) + server (PID 23864) running fresh (20:49:37); overlay.log shows
    [TRAY-CS] healthy + server live + SMTC/OBS watchers active -> app healthy. overlay.html UNCHANGED
    (not touched) -> the overlay still consumes its nested config via the existing translator; saveConfig/
    previewConfig now post the FULL merged nested (contract preserved + layout now carried). Live OBS
    tweak verification is the operator gate (STEP 6).
  - version.json re-stamped by rebuild (version 14.0.0, new msi_sha256) -> `git checkout -- version.json`
    -> restored to committed shipping bytes (14.0.0, msi_sha256 7dcd4e34). NO version bump.

## S5.3 Commit (log only)
Commit: `Stage 7.32: STEP 5 -- cold rebuild SE2 clean + install sanity (customize.html 6DCA6F9A bundled, tray+server healthy) + version.json restored 14.0.0; round-trip/bindings intact`

== END OF STEP 5 ==

# STEP 6 -- pre-gate + operator gate (HALT, SE4)

## S6.1 Pre-gate (Ruflo-side) -- ALL PASS
1. SCOPE: only src/customize.html changed (33D09CDF -> 6DCA6F9A). 4 protected (tray.ps1 19011F0B /
   tray_native.cs 6B9804A1 / launcher.cs 291ED4C9 / server.js C15ED931) + customize_legacy.html
   7E98377D + overlay.html 9A7CC817 == baseline (UNCHANGED).
2. version.json = 14.0.0 (no bump; re-stamped msi_sha256 restored to shipping 7dcd4e34). Real
   MasterShadex/Masters-FM remote UNTOUCHED this stage (no git push; the "ahead 355" + old reflog push
   entries are pre-existing local history from prior sessions).
3. Categories == V1's 17 (node verifier: names/order/icons from 1A; 120/120 controls bucketed per 1B).
   Settings gap = NONE (Item 3 SKIP) -> full V1 set present.
4. Import/export (evidence/s7_32/import_export_compat.json): new->new LOSSLESS incl LAYOUT
   (export1===export2, node geometry carried); V1-format friend envelope imports + applies; malformed
   rejected.
5. Big text gone (welcome banner CSS/HTML/JS + reshow link removed); tooltips = only help surface
   (95 help fields; help-icon engine intact).
6. Round-trip self-test ok; cold rebuild SE2 clean; tray+server healthy; overlay.html unchanged ->
   nested contract held (saveConfig/previewConfig post the full merged nested).

## S6.2 Operator gate -- HALT for PASS/FAIL (presented to operator).

== STEP 6 (HALT for operator) ==

## S6.2 OPERATOR GATE -> FAIL (strike 1/3): "the card is the entire display. it is supposed to be 1000 x 200"

### SE5 DIAGNOSIS (look before fix)
- Live /overlay-config (disk, what the overlay loads): layout.enabled=FALSE, NO layout.canvas,
  masters.overallSize=100 (-> --overall-scale 1.0). Screenshot shows NORMAL-mode card (art left /
  text / spectrum right / progress bottom) blown up to ~16:9 -- NOT layout-mode scatter.
- overlay.html (UNCHANGED this stage, SHA 9A7CC817, OUT OF SCOPE) renders the card to FILL its OBS
  Browser Source 100%: html,body width/height:100%; #scale-wrapper 200%x200% "responsive to any OBS
  browser source size" (line 162-168); comment 172-174 "the card fills the OBS Browser Source
  (1000x200 default)". So card size == OBS source size, always.
- applyLayoutMode (overlay 1695): when layout.enabled=false it STRIPS all inline geometry + returns ->
  the disabled+no-canvas config cannot drive a huge card.
- Send-diff (build_tools/_s732_senddiff.js) old(flat-only) vs new(deepMerge) for the live config:
  layout.enabled false in BOTH; only NEW-only keys are unmapped non-sizing (lastPresetName etc.).
  => 7.32 customize.html payload is SIZE-NEUTRAL.
- CONCLUSION: customize.html does not and cannot set the OBS Browser Source size; 7.32's changes are
  size-neutral. The card filling the display = the OBS Browser Source is larger than 1000x200, driven
  by overlay.html (unchanged, out of scope) + the OBS source dimensions / OBS-source-side integration
  (a known open issue: "OBS Source Side unfinished"). NOT a customize.html 7.32 defect.
- Did NOT hack customize.html (would be a spurious out-of-scope fix). Pending operator confirmation of
  the OBS source dimensions + whether the card was 1000x200 before this rebuild. If reproducible with
  the source AT 1000x200, will browser-inspect + dig deeper.

== STEP 6: FAIL (strike 1) -- SE5 diagnosis logged; awaiting operator OBS-source confirmation ==
